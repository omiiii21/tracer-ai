// Minimal stub — full implementation lands in Task 2 of plan 03-08.
export function Chat() {
  return (
    <div className="p-8 text-sm text-muted-foreground">
      Chat page (placeholder — full UI implementation lands shortly).
    </div>
  );
}
