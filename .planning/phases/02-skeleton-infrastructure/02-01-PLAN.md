---
phase: 02-skeleton-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .dockerignore
  - .env.example
  - tracer_ai/__init__.py
  - tracer_ai/errors.py
  - tracer_ai/tracer/__init__.py
  - tracer_ai/tracer/span.py
  - tracer_ai/tracer/context.py
  - tracer_ai/tracer/store.py
  - tracer_ai/tracer/exporters/__init__.py
  - tracer_ai/tracer/exporters/postgres.py
  - tracer_ai/rag/__init__.py
  - tracer_ai/eval/__init__.py
  - tracer_ai/corpus/__init__.py
  - tracer_ai/api/__init__.py
  - tracer_ai/cli/__init__.py
  - tracer_ai/cli/__main__.py
  - tracer_ai/cli/partition.py
  - tests/__init__.py
  - tests/test_imports.py
  - tests/conftest.py
autonomous: false  # contains a [BLOCKING] checkpoint task for Voyage pricing verification (ADR 003 prereq)
requirements:
  - INFRA-01
  - INFRA-03

tags:
  - python-scaffold
  - uv
  - pyproject
  - pydantic-v2
  - tracer-ai

must_haves:
  truths:
    - "Repo root contains tracer_ai/, frontend/ (created Wave 4), infra/ (created Wave 2), alembic/ (created Wave 3) — flat layout per D-2.01"
    - "tracer_ai/__init__.py exposes ONLY __version__ (PEP 396) per D-2.02"
    - "tracer_ai package mirrors docs/architecture.md §Recommended Project Structure: tracer/, rag/, eval/, corpus/, api/, cli/ + errors.py + (config.py created in Wave 4)"
    - "pyproject.toml declares requires-python = '>=3.12,<3.13' per D-2.06"
    - "pyproject.toml [project.dependencies] lists every locked runtime dep per D-2.07; [project.optional-dependencies].dev lists tooling deps"
    - "uv.lock is generated and committed per D-2.05"
    - ".env.example committed; .env in .gitignore per D-2.22"
    - ".env.example values are placeholders only (sk-ant-REPLACE etc.); real secrets never appear per D-2.23"
    - "tracer_ai/tracer/span.py contains the gen_ai.* attribute name constants block from docs/trace-schema.md (Phase 4 fills emission helpers); gen_ai.system appears ONLY on a single comment-out line marked DEPRECATED per D-2.40"
    - "Voyage AI pricing verified before voyageai dep ships (ADR 003 prereq) — 200M tokens free per account per RESEARCH.md Topic 8 confirms no spend required"
    - "Open Question Q2 (flat vs nested Settings) addendum: Wave 4 plan adopts FLAT Settings shape per RESEARCH.md Topic 5 recommendation; D-2.20 nested rationale revisited; saved nested grouping is a future revision"
    - "Open Question Q1 (import-linter vs custom guard) addendum: ship D-2.27 custom guard for portfolio narrative; .importlinter alternative documented in Wave 5 plan as future swap"
  artifacts:
    - path: "pyproject.toml"
      provides: "Backend dep manifest, ruff/mypy/pytest config tables"
      contains: 'requires-python = ">=3.12,<3.13"'
    - path: "uv.lock"
      provides: "Reproducible dep resolution"
    - path: ".env.example"
      provides: "Env var contract for operator setup"
      contains: "ANTHROPIC_API_KEY=sk-ant-REPLACE"
    - path: ".gitignore"
      provides: "Excludes .env, .venv, __pycache__, .testmondata, node_modules, dist, .pytest_cache, .mypy_cache, .ruff_cache"
    - path: ".dockerignore"
      provides: "Mirrors .gitignore + node_modules + .venv per D-2.14"
    - path: "tracer_ai/__init__.py"
      provides: "Package version export"
      contains: '__version__'
      min_lines: 2
    - path: "tracer_ai/tracer/span.py"
      provides: "OTel-aligned gen_ai.* attribute name constants stub (Phase 4 fills emission helpers)"
      contains: "gen_ai.provider.name"
    - path: "tracer_ai/cli/partition.py"
      provides: "create_next_month_partition() helper stub per D-2.18"
      contains: "create_next_month_partition"
    - path: "tests/test_imports.py"
      provides: "Smoke-import test covering INFRA-01 (Wave 0 gap from RESEARCH.md §Validation Architecture)"
  key_links:
    - from: "pyproject.toml [project.dependencies]"
      to: "Wave 2 Dockerfile.backend deps stage"
      via: "uv sync --frozen --no-install-project --all-extras"
      pattern: "uv sync"
    - from: "tracer_ai/__init__.py __version__"
      to: "Wave 4 tracer_ai/api/main.py FastAPI(version=__version__)"
      via: "from tracer_ai import __version__"
      pattern: "from tracer_ai import __version__"
---

<objective>
Lay down the canonical Python repo scaffold the rest of Phase 2 builds on: `pyproject.toml` with locked deps + `uv.lock`, the `tracer_ai/` package mirror of `docs/architecture.md`, stub modules carrying the OTel-aligned attribute constants from `docs/trace-schema.md`, the `.env.example` env-var contract, the `.gitignore`/`.dockerignore` exclusions, and a Wave-0 smoke-import test (`tests/test_imports.py`) that proves INFRA-01 once Wave 4's `config.py` lands. Voyage AI pricing is verified at the top of the plan as the explicit ADR 003 prereq before shipping the `voyageai` dep — this is a [BLOCKING] checkpoint, not a free pass.

Purpose: Wave 1 has zero dependencies. It must be repo scaffold first so every subsequent wave (b Compose → c Alembic → d FastAPI → e frontend → f pre-commit) operates against a real package layout, real `pyproject.toml`, and a real env-var contract — no scavenger hunts, no "where does config.py go" rediscovery.

Output: Repo skeleton ready for Wave 2 to mount as build context; `uv.lock` ready for Wave 2's Dockerfile `deps` layer; `.env.example` ready for Wave 2's compose `env_file:` directive; `tests/test_imports.py` is the Wave-0 gap that Wave 4 will green when `tracer_ai/config.py` ships.
</objective>

<execution_context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/PROJECT.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/ROADMAP.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/STATE.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/REQUIREMENTS.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/CLAUDE.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/architecture.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/module-deps.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/trace-schema.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/003-embedding-provider.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/005-observability-strategy.md

<interfaces>
<!-- Wave 1 creates new interfaces. Stub contracts are minimal; downstream waves consume them. -->

tracer_ai/__init__.py exports:
```python
__version__: str  # e.g., "0.1.0"
```

tracer_ai/tracer/span.py exports (Phase 2 stub — Phase 4 TRCR-01 fills implementation):
```python
# OTel GenAI attribute name constants (per docs/trace-schema.md §"Python attribute constants")
GEN_AI_PROVIDER_NAME: str  # "gen_ai.provider.name"  (NOT gen_ai.system — DEPRECATED, see comment-out line)
GEN_AI_OPERATION_NAME: str
GEN_AI_REQUEST_MODEL: str
GEN_AI_USAGE_INPUT_TOKENS: str
GEN_AI_USAGE_OUTPUT_TOKENS: str
# rag.* custom attribute name constants
RAG_RETRIEVED_CHUNKS: str
RAG_RETRIEVAL_SCORE_MEAN: str
RAG_RETRIEVAL_SCORE_MIN: str
RAG_PROMPT_TEMPLATE_ID: str
RAG_EVAL_FAITHFULNESS: str
RAG_EVAL_RELEVANCE: str
RAG_EVAL_JUDGE_MODEL: str
RAG_EVAL_JUDGE_PROMPT_VERSION: str
RAG_EVAL_JUDGE_COST_USD: str
```

tracer_ai/cli/partition.py exports (Phase 2 stub):
```python
def create_next_month_partition() -> None:
    """Phase 2 placeholder. Wave 3 Alembic plan does NOT depend on this; it is a CLI helper invocable as `python -m tracer_ai.cli partition create-next-month`. Body lands in Phase 7 polish."""
    raise NotImplementedError("Phase 2 stub — body lands in Phase 7 polish per D-2.18")
```
</interfaces>

</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0 [BLOCKING]: Verify Voyage AI pricing prereq before shipping voyageai dep (ADR 003)</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/003-embedding-provider.md (the ADR carrying the "Mandatory follow-ups" pricing checkbox)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Out of scope" (Voyage pricing as Phase 2 prereq, not Phase 1 carry-over) and §"specifics" (operator confirms before INFRA-01 closes)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Topic 8: Voyage AI pricing verification" (RESEARCH.md verified 200M free tokens; $0.18/1M paid; 25M tokens for full corpus is well under free tier; no spend required)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/STATE.md §Blockers/Concerns (line 106 — "Voyage AI voyage-code-3 pricing not confirmed")
  </read_first>
  <what-built>
    Nothing yet — this gate runs BEFORE Task 1 ships the voyageai dep. RESEARCH.md Topic 8 already verified pricing on 2026-05-04: 200M free tokens, $0.18/1M paid for voyage-code-3. The operator just needs to acknowledge and tick the checkbox in the ADR.
  </what-built>
  <how-to-verify>
    1. Open https://docs.voyageai.com/docs/pricing in a browser.
    2. Confirm one of the following two statements is still true:
       (a) `voyage-code-3` is included under the 200M free token allowance (default expectation per RESEARCH.md Topic 8).
       (b) The operator accepts the current paid rate.
    3. Edit `docs/decisions/003-embedding-provider.md` and tick the "Mandatory follow-ups" pricing checkbox, recording the verification date `2026-05-04` (or current date) inline.
    4. Reply with one of:
       - `approved` (free tier still applies; proceed)
       - `paid-accepted` (paid rate accepted; proceed)
       - `blocked: <reason>` (pricing changed unfavorably; planner will replan to use sentence-transformers offline fallback only)
  </how-to-verify>
  <resume-signal>Type one of: `approved`, `paid-accepted`, or `blocked: <reason>`.</resume-signal>
  <acceptance_criteria>
    - `grep -c "checked.*pricing" C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/003-embedding-provider.md` returns ≥ 1 (the ticked-checkbox line in "Mandatory follow-ups" section).
    - Operator response captured in plan execution log; one of the three resume signals received.
  </acceptance_criteria>
  <done>
    Operator approval recorded; planner unblocks Task 1. If `blocked:`, executor pauses and surfaces to planner for replan (out of scope for this plan).
  </done>
</task>

<task type="auto">
  <name>Task 1: Author pyproject.toml + uv.lock with locked deps and tool config</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/pyproject.toml (file does not yet exist; this task creates it — confirm absence with `ls`)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/CLAUDE.md §"Technology Stack" (locked stack table) and §"Supporting Libraries" (Backend Python — anthropic 0.49+, voyageai 0.3+, sentence-transformers 3.x, pgvector 0.3+, asyncpg 0.29+, sqlalchemy 2.0+ asyncio, alembic 1.x, uvicorn 0.30+, structlog 24.x, tiktoken 0.7+, httpx 0.27+, python-multipart latest)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Python Dependency Manager (D-2.05..D-2.07)" (D-2.05 uv; D-2.06 requires-python; D-2.07 dep groups)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Pre-commit & Tooling (D-2.24..D-2.28)" (D-2.25 ruff config; D-2.26 mypy config)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Standard Stack" (verified version table) and §"Validation Architecture" (asyncio_mode auto + testpaths)
  </read_first>
  <action>
Create `C:/Users/om.mengshetti/Desktop/tracer-ai/pyproject.toml` with the EXACT content below. Per Open Question Q1 / RESEARCH.md Topic 9 recommendation: planner ships D-2.27 custom guard (no `import-linter` dep) — narrative wins for portfolio. Per Open Question Q2: Settings shape will be FLAT in Wave 4 (no nested namespaces); deps list is unaffected.

```toml
[project]
name = "tracer-ai"
version = "0.1.0"
description = "Portfolio-grade RAG chatbot with custom OTel-aligned observability"
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "MIT" }
authors = [{ name = "tracer-ai contributors" }]
dependencies = [
  "fastapi>=0.128,<0.129",
  "pydantic>=2.7,<3.0",
  "pydantic-settings>=2.4,<3.0",
  "sqlalchemy[asyncio]>=2.0,<2.1",
  "asyncpg>=0.29,<0.31",
  "pgvector>=0.3,<0.4",
  "alembic>=1.13,<2.0",
  "uvicorn[standard]>=0.30,<0.40",
  "anthropic>=0.49,<1.0",
  "voyageai>=0.3,<1.0",
  "sentence-transformers>=3.0,<4.0",
  "structlog>=24.1,<25.0",
  "tiktoken>=0.7,<1.0",
  "httpx>=0.27,<0.30",
  "python-multipart>=0.0.9,<1.0",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.7,<1.0",
  "mypy>=1.11,<2.0",
  "pytest>=8.3,<9.0",
  "pytest-asyncio>=0.23,<1.0",
  "pytest-testmon>=2.2,<3.0",
  "pre-commit>=3.7,<5.0",
  "types-PyYAML",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tracer_ai"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["B011"]  # allow assert in tests

[tool.mypy]
python_version = "3.12"
strict = true
files = ["tracer_ai"]
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["voyageai.*", "pgvector.*", "sentence_transformers.*", "tiktoken.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q --strict-markers"

[tool.uv]
# uv-specific config (uv.lock generation)
```

Then run `uv lock` (or, if uv is not on PATH, install it: `pip install uv && uv lock`) to generate `C:/Users/om.mengshetti/Desktop/tracer-ai/uv.lock`. Both files must be committed.

Note: the empty `[tool.uv]` table is intentional and present so future uv-specific keys (e.g., `dev-dependencies`, `index-strategy`) have a stable home.
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      test -f pyproject.toml && \
      test -f uv.lock && \
      grep -q 'requires-python = ">=3.12,<3.13"' pyproject.toml && \
      grep -q '"voyageai>=0.3,<1.0"' pyproject.toml && \
      grep -q '"sqlalchemy\[asyncio\]>=2.0,<2.1"' pyproject.toml && \
      grep -q '"pgvector>=0.3,<0.4"' pyproject.toml && \
      grep -q 'asyncio_mode = "auto"' pyproject.toml && \
      grep -q '\[tool.mypy\]' pyproject.toml && \
      grep -q 'strict = true' pyproject.toml && \
      ! grep -q ':latest' pyproject.toml
    </automated>
  </verify>
  <acceptance_criteria>
    - File `C:/Users/om.mengshetti/Desktop/tracer-ai/pyproject.toml` exists.
    - File `C:/Users/om.mengshetti/Desktop/tracer-ai/uv.lock` exists.
    - `grep -c '^requires-python = ">=3.12,<3.13"' pyproject.toml` returns 1.
    - `grep -c '"voyageai>=' pyproject.toml` returns 1.
    - `grep -c '"sentence-transformers>=' pyproject.toml` returns 1.
    - `grep -c '"anthropic>=' pyproject.toml` returns 1.
    - `grep -c 'asyncio_mode = "auto"' pyproject.toml` returns 1.
    - `grep -c 'strict = true' pyproject.toml` returns ≥ 1.
    - `grep -c ':latest' pyproject.toml` returns 0.
    - `grep -c 'class Config:' pyproject.toml` returns 0 (Pydantic v1 idiom forbidden — D-2.39).
  </acceptance_criteria>
  <done>
    `pyproject.toml` and `uv.lock` are committed; both files satisfy all acceptance criteria.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tracer_ai package skeleton with stub modules and OTel constants</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/architecture.md §"Component Responsibilities" (module path table)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/module-deps.md (locked DAG: config + errors → tracer + corpus → rag + eval → api + cli; api/cli zero outgoing edges)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/trace-schema.md (lines 22-49 — the Python attribute-constants block — Phase 2 stub copies the constant NAMES; Phase 4 TRCR-01 fills emission helpers)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Repo Layout (D-2.01..D-2.04)" (D-2.01 flat layout; D-2.02 __init__.py exposes only __version__)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Anti-Patterns (D-2.36..D-2.40)" (D-2.40 — no `gen_ai.system` constant outside the DEPRECATED comment-out line; D-2.37 — no `print()` in `tracer_ai/`)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/005-observability-strategy.md (NO opentelemetry-sdk runtime; constants only)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Recommended Project Structure" (verbatim file list at lines 204-271 of RESEARCH.md)
  </read_first>
  <action>
Create the following files at the listed paths. All file bodies are stubs (placeholders) — Phase 2 only ships the SHAPE; bodies fill in Phase 4+ per per-module ownership.

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/__init__.py`** (per D-2.02: ONLY `__version__`)
```python
"""tracer-ai: portfolio-grade RAG chatbot with custom OTel-aligned observability."""

__version__ = "0.1.0"
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/errors.py`** (cross-cutting; module-deps.md leaf)
```python
"""Cross-cutting error types (Phase 2 stub).

errors.py is a leaf module per docs/module-deps.md — zero imports from sibling
tracer_ai modules. Bodies fill in Phase 3+ as adapters land.
"""


class TracerAIError(Exception):
    """Base exception for tracer-ai. Subclasses defined per phase as needed."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/__init__.py`**
```python
"""tracer/: Span dataclass, context propagation, async queue + Postgres writer.

Phase 2 ships stubs only. Phase 4 TRCR-01..10 fills bodies.
"""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/span.py`** (per D-2.40 + docs/trace-schema.md)
```python
"""OTel-aligned + RAG-specific attribute name constants (Phase 2 stub).

This module ships ONLY the attribute-name constants block from
docs/trace-schema.md §"Python attribute constants". Span emission helpers,
dataclasses, and lifecycle methods land in Phase 4 TRCR-01..04.

Per ADR 005: NO `opentelemetry-sdk` runtime dependency. Constants are bare
strings; downstream code consumes them as JSONB attribute keys.

Per D-2.40 / ADR 005: `gen_ai.system` is DEPRECATED in the OTel GenAI spec.
Use `gen_ai.provider.name` (= "anthropic") for tracer-ai's LLM spans. The
`gen_ai.system` line below is COMMENTED OUT and tagged DEPRECATED so the
pre-commit grep gate (Wave 5) does not flag this file.
"""

# DEPRECATED: gen_ai.system  (kept commented-out for posterity; D-2.40)

# OTel GenAI namespace (Development stability — see docs/trace-schema.md)
GEN_AI_PROVIDER_NAME: str = "gen_ai.provider.name"
GEN_AI_OPERATION_NAME: str = "gen_ai.operation.name"
GEN_AI_REQUEST_MODEL: str = "gen_ai.request.model"
GEN_AI_USAGE_INPUT_TOKENS: str = "gen_ai.usage.input_tokens"
GEN_AI_USAGE_OUTPUT_TOKENS: str = "gen_ai.usage.output_tokens"

# rag.* custom namespace (tracer-ai-specific; see docs/trace-schema.md)
RAG_RETRIEVED_CHUNKS: str = "rag.retrieved_chunks"
RAG_RETRIEVAL_SCORE_MEAN: str = "rag.retrieval.score.mean"
RAG_RETRIEVAL_SCORE_MIN: str = "rag.retrieval.score.min"
RAG_PROMPT_TEMPLATE_ID: str = "rag.prompt_template.id"
RAG_EVAL_FAITHFULNESS: str = "rag.eval.faithfulness"
RAG_EVAL_RELEVANCE: str = "rag.eval.relevance"
RAG_EVAL_JUDGE_MODEL: str = "rag.eval.judge_model"
RAG_EVAL_JUDGE_PROMPT_VERSION: str = "rag.eval.judge_prompt_version"
RAG_EVAL_JUDGE_COST_USD: str = "rag.eval.judge_cost_usd"

# feedback namespace
FEEDBACK_DIAGNOSIS_TAG: str = "feedback.diagnosis_tag"
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/context.py`**
```python
"""Context propagation helpers (Phase 2 stub; Phase 4 TRCR-04 fills).

Per docs/sequence-diagrams.md (Pitfall #1 mitigation): the eval branch will
snapshot OTel context BEFORE rag.request root span ends so rag.eval becomes
a child span, not an orphan root.
"""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/store.py`**
```python
"""TraceStore Protocol (Phase 2 stub; Phase 4 TRCR-05 fills).

Per docs/architecture.md: TraceStore is the persistence boundary; the
Postgres adapter lives in tracer/exporters/postgres.py.
"""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/exporters/__init__.py`**
```python
"""Trace exporters package (Phase 2 stub; Phase 4 TRCR-06 fills postgres.py)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/tracer/exporters/postgres.py`**
```python
"""Postgres+JSONB trace exporter (Phase 2 stub; Phase 4 TRCR-06 fills body).

Per ADR 004: bounded asyncio.Queue(maxsize=1000), background consumer
batches inserts, lifespan force-flushes on shutdown.
"""
```

**Empty package files** (each contains ONLY the docstring shown):

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/rag/__init__.py`**
```python
"""rag/: Pipeline orchestrator + LLM Protocol + Anthropic adapter (Phase 3)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/eval/__init__.py`**
```python
"""eval/: LLM-as-judge worker + RAGAS-style prompts (Phase 5)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/corpus/__init__.py`**
```python
"""corpus/: Markdown chunker + Voyage embedder + pgvector retriever (Phase 3)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/api/__init__.py`**
```python
"""api/: FastAPI routes + Pydantic schemas (Wave 4 fills)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/cli/__init__.py`**
```python
"""cli/: Typer-based command surface (Phase 6 fills full body)."""
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/cli/__main__.py`** (allowlisted to use print per D-2.37)
```python
"""tracer-ai CLI entry point (Phase 6 fills body).

Per D-2.37: print() is allowed in this file (CLI output goes to stdout); pre-commit
grep allowlist exempts tracer_ai/cli/__main__.py.
"""

if __name__ == "__main__":
    print("tracer-ai CLI: Phase 6 will fill this in. See README for current usage.")
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tracer_ai/cli/partition.py`** (per D-2.18)
```python
"""Monthly partition rotation helper (Phase 2 stub; body lands in Phase 7 polish).

Invocable as `python -m tracer_ai.cli partition create-next-month`. Phase 2
installs the helper but does not wire a cron — operator runs it monthly.
"""


def create_next_month_partition() -> None:
    """Create the next forward-rolling monthly partition for the spans table.

    Phase 2 stub. Phase 7 polish fills body.
    """
    raise NotImplementedError(
        "Phase 2 stub. Phase 7 polish fills body per D-2.18."
    )
```
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      python -c "import tracer_ai; assert tracer_ai.__version__ == '0.1.0', tracer_ai.__version__" && \
      python -c "import tracer_ai.tracer.span as s; assert s.GEN_AI_PROVIDER_NAME == 'gen_ai.provider.name'" && \
      python -c "import tracer_ai.tracer.span as s; assert not hasattr(s, 'GEN_AI_SYSTEM')" && \
      python -c "from tracer_ai.cli.partition import create_next_month_partition; import pytest" && \
      python -c "import tracer_ai.errors, tracer_ai.tracer, tracer_ai.tracer.context, tracer_ai.tracer.store, tracer_ai.tracer.exporters, tracer_ai.tracer.exporters.postgres, tracer_ai.rag, tracer_ai.eval, tracer_ai.corpus, tracer_ai.api, tracer_ai.cli, tracer_ai.cli.partition" && \
      grep -v '^#' tracer_ai/tracer/span.py | grep -c 'gen_ai\.system' | grep -q '^0$'
    </automated>
  </verify>
  <acceptance_criteria>
    - All 14 files exist at their absolute paths.
    - `python -c "import tracer_ai; assert tracer_ai.__version__"` exits 0.
    - `grep -c "^__version__" tracer_ai/__init__.py` returns 1.
    - `grep -c "GEN_AI_PROVIDER_NAME" tracer_ai/tracer/span.py` returns ≥ 1.
    - `grep -v '^#' tracer_ai/tracer/span.py | grep -c 'gen_ai\.system'` returns 0 (the only `gen_ai.system` mention is on the commented-out DEPRECATED line and is filtered out by the comment-stripping `grep -v '^#'`).
    - `grep -rE '^\s*print\(' tracer_ai/ | grep -v '^tracer_ai/cli/__main__.py'` returns empty (D-2.37 — allowlist for cli/__main__.py only).
    - All 14 modules importable with no `ModuleNotFoundError`.
  </acceptance_criteria>
  <done>
    `tracer_ai/` package skeleton complete; module-deps.md DAG implementable in Wave 5 import-cycle guard; OTel attribute constants ready for Phase 4 to consume.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author .gitignore, .dockerignore, .env.example, and Wave-0 test scaffolding</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Environment Variables (D-2.19..D-2.23)" (D-2.19 required vars; D-2.22 .env gitignored + .env.example committed; D-2.23 placeholders only)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Docker Image Strategy (D-2.11..D-2.14)" (D-2.14 .dockerignore mirrors .gitignore + node_modules + .venv etc.)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Validation Architecture" (Wave 0 gaps: tests/test_imports.py, tests/conftest.py)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Topic 5" (.env.example contents at lines 846-860)
  </read_first>
  <action>

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/.gitignore`**
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
env/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.testmondata
.coverage
htmlcov/

# Node / frontend
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Env / secrets — NEVER commit real .env (D-2.22)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/.dockerignore`** (per D-2.14)
```
# Mirrors .gitignore
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.testmondata
.coverage
htmlcov/

# Node
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.local
.env.*.local

# Build context bloat
.git/
.github/
.planning/
.claude/
docs/
*.md
!README.md

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/.env.example`** (per D-2.22 + D-2.23 + RESEARCH.md Topic 5 lines 846-860)
```
# tracer-ai env-var contract (committed; copy to .env and edit)
# Per D-2.21: all REQUIRED vars are validated at import time; missing => ValidationError.
# Per D-2.23: NEVER commit a real value here. Placeholders only.

# === Required (fail-fast on missing) ===
DATABASE_URL=postgresql+asyncpg://tracer:tracer@db:5432/tracer_ai
ANTHROPIC_API_KEY=sk-ant-REPLACE
VOYAGE_API_KEY=REPLACE

# === Optional with defaults ===
LLM_BOT_MODEL=claude-sonnet-4-5-20250929
LLM_JUDGE_MODEL=claude-haiku-4-5-20251001
EMBEDDING_MODEL=voyage-code-3
LOG_LEVEL=INFO
ENABLE_RERANKER=false
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tests/__init__.py`** (empty file — package marker)
```python
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tests/conftest.py`**
```python
"""pytest configuration (Phase 2 Wave 0 scaffold)."""

import sys
from collections.abc import Iterator

import pytest


@pytest.fixture
def clean_env(monkeypatch: pytest.MonkeyPatch) -> Iterator[None]:
    """Clear tracer-ai env vars AND evict tracer_ai.config from sys.modules.

    Used by tests/test_config_failfast.py (Wave 4) to verify Settings raises
    ValidationError when required vars are missing.

    Per fix W-6: this fixture also evicts `tracer_ai.config` from sys.modules.
    Module-top-level `settings = Settings()` only re-runs validation when the
    module is fresh-imported. If pytest collection imports `tracer_ai.config`
    once with valid env vars, the cached module survives subsequent fixture
    runs and the fail-fast guarantee silently regresses. Tests should ALSO call
    `sys.modules.pop("tracer_ai.config", None)` immediately before
    `importlib.import_module("tracer_ai.config")` for safety; this fixture
    establishes the baseline.
    """
    for key in (
        "DATABASE_URL",
        "ANTHROPIC_API_KEY",
        "VOYAGE_API_KEY",
        "LLM_BOT_MODEL",
        "LLM_JUDGE_MODEL",
        "EMBEDDING_MODEL",
        "LOG_LEVEL",
        "ENABLE_RERANKER",
    ):
        monkeypatch.delenv(key, raising=False)
    # Evict the cached module so the next import re-runs `settings = Settings()`
    # at module top level (the fail-fast point per D-2.21).
    sys.modules.pop("tracer_ai.config", None)
    yield
    # monkeypatch auto-restores env vars; we leave sys.modules state alone
    # post-yield because subsequent tests should explicitly pop again if they
    # need the fresh-import behavior (defensive depth).
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/tests/test_imports.py`**
```python
"""Smoke-import test (covers INFRA-01 per RESEARCH.md §Validation Architecture).

This test was Wave-0 in the validation architecture: it MUST pass once Wave 4
ships tracer_ai/config.py with the Settings class. Until then, the
config-import test is parametrized to skip gracefully.
"""

import importlib

import pytest


def test_package_version_exposed() -> None:
    """tracer_ai package exposes only __version__ (D-2.02)."""
    import tracer_ai

    assert hasattr(tracer_ai, "__version__")
    assert isinstance(tracer_ai.__version__, str)
    assert tracer_ai.__version__ == "0.1.0"


@pytest.mark.parametrize(
    "module_name",
    [
        "tracer_ai.errors",
        "tracer_ai.tracer",
        "tracer_ai.tracer.span",
        "tracer_ai.tracer.context",
        "tracer_ai.tracer.store",
        "tracer_ai.tracer.exporters",
        "tracer_ai.tracer.exporters.postgres",
        "tracer_ai.rag",
        "tracer_ai.eval",
        "tracer_ai.corpus",
        "tracer_ai.api",
        "tracer_ai.cli",
        "tracer_ai.cli.partition",
    ],
)
def test_module_importable(module_name: str) -> None:
    """Every module in the tracer_ai/ tree must be importable (INFRA-01)."""
    importlib.import_module(module_name)


def test_otel_attribute_constants_present() -> None:
    """Phase 2 stub of tracer/span.py carries the OTel constants block (D-2.40).

    `gen_ai.system` MUST NOT appear as a runtime constant — only as a
    commented-out DEPRECATED line, which Python doesn't expose on the module.
    """
    from tracer_ai.tracer import span

    assert span.GEN_AI_PROVIDER_NAME == "gen_ai.provider.name"
    assert not hasattr(span, "GEN_AI_SYSTEM")
    # Spot-check rag.* constants
    assert span.RAG_EVAL_FAITHFULNESS == "rag.eval.faithfulness"
    assert span.RAG_PROMPT_TEMPLATE_ID == "rag.prompt_template.id"


def test_partition_helper_stub_raises() -> None:
    """Phase 2 ships partition.create_next_month_partition() as a stub raising NotImplementedError (D-2.18)."""
    from tracer_ai.cli.partition import create_next_month_partition

    with pytest.raises(NotImplementedError):
        create_next_month_partition()
```

Run `cd C:/Users/om.mengshetti/Desktop/tracer-ai && uv sync --all-extras && uv run pytest -q tests/test_imports.py` to install deps and verify the smoke tests pass against the Wave 1 scaffold.
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      test -f .gitignore && \
      test -f .dockerignore && \
      test -f .env.example && \
      test -f tests/test_imports.py && \
      test -f tests/conftest.py && \
      test -f tests/__init__.py && \
      grep -q '^\.env$' .gitignore && \
      grep -q '^node_modules/' .gitignore && \
      grep -q '\.testmondata' .gitignore && \
      grep -q '^\.env$' .dockerignore && \
      grep -q '^node_modules/' .dockerignore && \
      grep -q '^DATABASE_URL=postgresql\+asyncpg://' .env.example && \
      grep -q '^ANTHROPIC_API_KEY=sk-ant-REPLACE$' .env.example && \
      grep -q '^VOYAGE_API_KEY=REPLACE$' .env.example && \
      grep -q '^EMBEDDING_MODEL=voyage-code-3$' .env.example && \
      grep -cE 'sk-ant-[a-zA-Z0-9_-]{20,}' .env.example | grep -q '^0$' && \
      uv sync --all-extras && \
      uv run pytest -q tests/test_imports.py
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.gitignore` exists, contains `.env`, `node_modules/`, `__pycache__/`, `.testmondata`, `.venv/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `dist/`.
    - File `.dockerignore` exists, mirrors `.gitignore` plus excludes `.git/`, `.planning/`, `.claude/`, `docs/`.
    - File `.env.example` exists, contains exactly the 8 lines `DATABASE_URL=...`, `ANTHROPIC_API_KEY=sk-ant-REPLACE`, `VOYAGE_API_KEY=REPLACE`, `LLM_BOT_MODEL=...`, `LLM_JUDGE_MODEL=...`, `EMBEDDING_MODEL=voyage-code-3`, `LOG_LEVEL=INFO`, `ENABLE_RERANKER=false` (plus comments).
    - `grep -cE 'sk-ant-[a-zA-Z0-9_-]{20,}' .env.example` returns 0 (placeholder is `sk-ant-REPLACE` which is < 20 chars after the prefix, so no false-positive on the gitleaks regex).
    - `uv sync --all-extras` exits 0 (deps install).
    - `uv run pytest -q tests/test_imports.py` exits 0; the 4 test functions pass (1 sole + 13 parametric module imports + 2 spot-checks = 16 test items).
  </acceptance_criteria>
  <done>
    Repo-level dotfiles + env contract + Wave-0 smoke-test scaffold committed; INFRA-01 partial coverage (Wave 4 closes config-failfast test); INFRA-03 partial coverage (.env.example placeholder discipline established).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → repo (`.env.example` template) | Operator copies and fills `.env`; risk is `.env.example` containing real credentials |
| pyproject.toml → PyPI | uv resolves deps from PyPI; risk is supply-chain swap on transitive deps |
| `tracer_ai/` source → consumers (Wave 4 api, alembic env.py) | API/migration consume `__version__`, `tracer_ai.config.settings` (Wave 4); risk is constants drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01-01 | Information Disclosure | `.env.example` accidentally containing a real `sk-ant-...` key | mitigate | Acceptance criteria assert `grep -cE 'sk-ant-[a-zA-Z0-9_-]{20,}'` returns 0; placeholder is `sk-ant-REPLACE` (8 chars after prefix; below the gitleaks threshold). Wave 5 plan adds `gitleaks` pre-commit hook with `.gitleaks.toml` allowlisting `.env.example` per RESEARCH.md Topic 11. |
| T-2-01-02 | Information Disclosure | `.env` accidentally committed (real secrets) | mitigate | `.gitignore` has `.env` line; Wave 5 gitleaks pre-commit hook scans staged content; pre-receive cannot block but local hooks make accidents loud. |
| T-2-01-03 | Tampering | Supply-chain attack on transitive deps in `uv.lock` | mitigate | uv.lock pins exact versions + hashes; Wave 5 plan adds `pre-commit-hooks check-added-large-files` (caps single-file size); D-2.36 forbids `:latest` Docker tags (separate threat surface, separate mitigation). Accepted residual: PyPI itself trusted as registry. |
| T-2-01-04 | Tampering | `tracer_ai/tracer/span.py` reintroduces `gen_ai.system` constant via copy-paste from external OTel example | mitigate | Acceptance criterion `grep -v '^#' tracer_ai/tracer/span.py | grep -c 'gen_ai\.system'` returns 0 — the comment-stripping pre-filter is the canonical pattern (avoids the self-invalidating grep gate where header comments trigger the gate); Wave 5 pre-commit grep gate enforces project-wide. |
| T-2-01-05 | Information Disclosure | Phase 2 leaks .planning/ artifacts into Docker image build context | mitigate | `.dockerignore` excludes `.planning/`, `.claude/`, `docs/`, `.git/` per D-2.14; build context size capped via Wave 2 image build verification. |
| T-2-01-06 | Spoofing | A non-tracer_ai module under `tracer_ai/` masquerades as a real adapter (e.g., adversarial PR adding `tracer_ai/rag/llm.py` with `from anthropic import Anthropic` outside the locked adapter file path) | accept | Phase 2 has no LLM SDK calls. D-2.38 grep enforcement lands in Wave 5 pre-commit. Risk is dormant in Phase 2; Wave 5 closes. |
| T-2-01-07 | Repudiation | `pyproject.toml` deps drift from CLAUDE.md "Locked Stack Validation" without commit trail | mitigate | Plan-phase commit message will reference D-2.07 + CLAUDE.md table; reviewers can `git log --follow pyproject.toml` to see decision provenance. |

ASVS Level 1 mapping: V8 Data Protection (T-2-01-01, T-2-01-02), V14 Configuration (T-2-01-03, T-2-01-05). All "mitigate" dispositions have specific implementation citations; accepted residual (T-2-01-06) closes in Wave 5.
</threat_model>

<verification>
Run from repo root after all 3 tasks complete:

```bash
cd C:/Users/om.mengshetti/Desktop/tracer-ai

# 1. Repo scaffold sanity
test -f pyproject.toml && test -f uv.lock
test -d tracer_ai && test -d tests
ls tracer_ai/ | sort
# Expected entries (alphabetical): __init__.py api cli corpus errors.py eval rag tracer

# 2. Dep manifest sanity
grep -cE '^\s*"(fastapi|pydantic|sqlalchemy\[asyncio\]|asyncpg|pgvector|alembic|uvicorn\[standard\]|anthropic|voyageai|sentence-transformers|structlog|tiktoken|httpx|python-multipart)' pyproject.toml
# Expected: ≥ 14 (one per locked dep)

grep -c '"\(ruff\|mypy\|pytest\|pytest-asyncio\|pytest-testmon\|pre-commit\)' pyproject.toml
# Expected: ≥ 6

# 3. No locked anti-patterns
grep -c ':latest' pyproject.toml             # → 0
grep -c 'class Config:' tracer_ai/           # → 0 (recursive)
grep -rEn '^\s*print\(' tracer_ai/ | grep -v '^tracer_ai/cli/__main__.py' | wc -l   # → 0
grep -v '^#' tracer_ai/tracer/span.py | grep -c 'gen_ai\.system'                    # → 0

# 4. Smoke imports green
uv sync --all-extras
uv run pytest -q tests/test_imports.py
# Expected: 16 passed

# 5. Env contract sanity
grep -E '^(DATABASE_URL|ANTHROPIC_API_KEY|VOYAGE_API_KEY|LLM_BOT_MODEL|LLM_JUDGE_MODEL|EMBEDDING_MODEL|LOG_LEVEL|ENABLE_RERANKER)=' .env.example | wc -l
# Expected: 8

grep -cE 'sk-ant-[a-zA-Z0-9_-]{20,}' .env.example
# Expected: 0 (placeholder is intentionally short)

# 6. ADR 003 prereq
grep -c "checked.*pricing\|pricing.*verified\|free tier" docs/decisions/003-embedding-provider.md
# Expected: ≥ 1 (Task 0 [BLOCKING] checkpoint ticked the checkbox)
```
</verification>

<success_criteria>
- Voyage pricing prereq ([BLOCKING] Task 0) explicitly resolved with operator approval recorded.
- `pyproject.toml` + `uv.lock` committed with locked deps; `requires-python = ">=3.12,<3.13"`.
- 14-file `tracer_ai/` package skeleton importable; `tracer_ai.__version__ == "0.1.0"`.
- `tracer_ai/tracer/span.py` carries OTel constants block; `gen_ai.system` only in DEPRECATED comment.
- `.gitignore`, `.dockerignore`, `.env.example` committed with placeholder values only.
- `tests/test_imports.py` (16 test items) green via `uv run pytest`.
- Wave 2 (Compose + Dockerfiles) can mount this scaffold as build context unchanged.
- Wave 4 plan can adopt FLAT Settings (per Open Question Q2) without modifying Wave 1 scaffold.
</success_criteria>

<output>
After completion, create `C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-01-SUMMARY.md` per the GSD summary template, including:
- Voyage pricing prereq resolution recorded
- All 14 `tracer_ai/` files listed with their stub status (Phase 4 / Phase 5 / Phase 6 / Phase 7 ownership)
- `pyproject.toml` dep table reproduced
- Open Question Q1 resolution: ship D-2.27 custom guard (deferred to Wave 5 plan)
- Open Question Q2 resolution: Wave 4 plan adopts FLAT Settings shape (deferred to Wave 4 plan)
- Wave 2 readiness: `pyproject.toml` + `uv.lock` ready as Dockerfile.backend `deps` stage build context
</output>
