---
phase: 02-skeleton-infrastructure
plan: 05
type: execute
wave: 4  # per fix W-1: parallel with Plan 02-04 (no files_modified overlap)
depends_on: [01, 02, 03]  # per fix W-1 / D-2.42: 02-04 and 02-05 both edit non-overlapping sections of infra/docker-compose.yml
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/components.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/button.tsx
  - infra/docker-compose.yml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

tags:
  - vite
  - react18
  - tailwind-v3
  - shadcn
  - frontend-skeleton

must_haves:
  truths:
    - "frontend/package.json pins react@^18.3.1, react-dom@^18.3.1 (NOT React 19), typescript@~5.5, vite@^5, tailwindcss@^3.4 (NOT v4), @tremor/react@^3, @tanstack/react-query@^5, react-router-dom@^6, clsx@^2, tailwind-merge@^2, lucide-react per D-2.30"
    - "frontend/tailwind.config.js uses Tailwind v3 syntax (content[], theme.extend, plugins[]) — NOT v4 @theme CSS-only syntax"
    - "frontend/components.json (shadcn) ships baseColor: zinc per Open Question Q4 (slate not in current shadcn CLI list); cssVariables: true; iconLibrary: lucide"
    - "shadcn-style Card and Button components scaffolded under frontend/src/components/ui/ per D-2.31; aliases @/* → ./src/* mirrored in BOTH tsconfig.json paths AND vite.config.ts resolve.alias per RESEARCH.md Topic 7"
    - "frontend/src/App.tsx renders <Card><CardHeader><CardTitle>Hello tracer-ai</CardTitle></CardHeader><CardContent>...</CardContent></Card> at the / route per D-2.29"
    - "frontend/.env (created from frontend/.env.example) carries VITE_API_BASE_URL — Wave 2 already shipped frontend/.env.example"
    - "Wave 2's docker-compose web service command updated from [sleep, infinity] placeholder to use the Dockerfile dev-stage CMD; placeholder REMOVED"
    - "Vite HMR works over Docker bind-mount: CHOKIDAR_USEPOLLING=true (compose) + server.watch.usePolling=true (vite.config.ts) per RESEARCH.md Topic 10 / Pitfall 4 — belt-and-suspenders"
    - "End-to-end: docker compose up brings web service up; curl http://localhost:5173/ returns 200 + index.html with <div id='root'> per Vite default"
    - "tsc --noEmit -p frontend/tsconfig.json exits 0 (Wave 5 plan adds this as a pre-commit hook)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Pinned dep manifest"
      contains: '"react": "^18.3.1"'
    - path: "frontend/vite.config.ts"
      provides: "Vite config with @/* alias + dev server config"
      contains: 'path.resolve(__dirname, "./src")'
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind v3 config (content + theme.extend + plugins)"
      contains: "content:"
    - path: "frontend/components.json"
      provides: "shadcn config (baseColor: zinc, lucide icons)"
      contains: '"baseColor": "zinc"'
    - path: "frontend/src/App.tsx"
      provides: "/ route hello-world Card"
      contains: "Hello tracer-ai"
    - path: "frontend/src/components/ui/card.tsx"
      provides: "shadcn Card primitive"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "shadcn Button primitive"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() helper"
      contains: "twMerge"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/ui/card.tsx + button.tsx"
      via: "import { Card, CardHeader, ... } from '@/components/ui/card'"
      pattern: "from \"@/components/ui/card\""
    - from: "frontend/vite.config.ts resolve.alias"
      to: "frontend/tsconfig.json compilerOptions.paths"
      via: "BOTH must alias @/* → ./src/* — drift breaks tsc --noEmit"
      pattern: "\"@/\\*\""
    - from: "infra/docker-compose.yml web service"
      to: "Dockerfile.frontend dev-stage CMD"
      via: "compose command override REMOVED → Dockerfile CMD runs `npm run dev -- --host 0.0.0.0`"
      pattern: "npm run dev"
---

<objective>
Ship the Vite + React 18 + TypeScript + Tailwind v3 + shadcn frontend skeleton (D-2.29 — minimal but Phase-3-ready, NOT vite-only-hello), the `Card` + `Button` shadcn components (D-2.31), the `/` route rendering "Hello tracer-ai" inside a Card (D-2.29), and the wiring that makes `tsc --noEmit` pass (so Wave 5's pre-commit hook has a real target). Update Compose to remove the Wave 2 `web` placeholder.

End of this wave: from a fresh checkout, `docker compose up` brings ALL services green (db + migrate + api + web); `curl http://localhost:8000/healthz` → 200 ok; `curl http://localhost:5173/` → 200 with `<div id="root">` HTML; opening `http://localhost:5173/` in a browser shows the "Hello tracer-ai" Card.

Purpose: Closes the frontend half of INFRA-02 + part of INFRA-01 (frontend layout). Wave 5 (pre-commit hooks) needs this to exist so `tsc --noEmit` and `tsc-frontend` hooks have something to lint.

Per Open Question Q4: `Zinc` is the shadcn base color (slate is no longer in the latest shadcn CLI list).
Per RESEARCH.md Topic 7 + Pitfall 5: pin React 18 + Tailwind v3 in package.json BEFORE first `npm install` (vite scaffold defaults to React 19 + Tailwind v4 in 2026).
</objective>

<execution_context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/PROJECT.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/CLAUDE.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/wireframes/README.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/docs/decisions/001-charting-library.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-02-PLAN.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-04-PLAN.md

<interfaces>
<!-- Wave 5 (this plan) writes a fresh frontend/ tree. Wave 2 shipped:
     - infra/Dockerfile.frontend (multi-stage with deps + dev + build + prod)
     - frontend/.env.example (VITE_API_BASE_URL=http://localhost:8000)
     - frontend/package.json STUB (which this plan REPLACES with the full pinned set)
     Wave 5 (pre-commit) consumes frontend/tsconfig.json paths from this plan. -->

frontend/package.json (REPLACES Wave 2 stub):
- pinned: react ^18.3.1, react-dom ^18.3.1, typescript ~5.5, vite ^5, tailwindcss ^3.4
- @tremor/react ^3 (Phase 5 uses; install Phase 2)
- @tanstack/react-query ^5 (Phase 3 uses; install Phase 2)
- react-router-dom ^6 (required for Phase 3 routes; Phase 2 ships single / route)
- clsx ^2, tailwind-merge ^2 (shadcn cn() utility)
- lucide-react latest (shadcn icon library)
- @types/react @types/react-dom @vitejs/plugin-react (dev)

frontend/components.json (shadcn config — Open Question Q4: baseColor=zinc):
{
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "src/index.css",
    "baseColor": "zinc",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}

frontend/src/App.tsx (D-2.29 + D-2.31):
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

export default function App() { return <Card>Hello tracer-ai</Card> ... }
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace frontend/package.json stub with pinned full set + tsconfig + vite/tailwind/postcss config</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/package.json (Wave 2 stub — to be REPLACED)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Frontend Skeleton Scope" D-2.29..D-2.32
    - C:/Users/om.mengshetti/Desktop/tracer-ai/CLAUDE.md §"Frontend JavaScript/TypeScript" (locked stack table)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Topic 7: shadcn CLI 3.x init in 2026" (vite.config.ts + components.json contents at lines 994-1029)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Open Questions" Q4 (baseColor=zinc)
  </read_first>
  <action>

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/package.json`** (REPLACES the Wave 2 stub)

```json
{
  "name": "tracer-ai-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.0.0",
    "@tremor/react": "^3.18.0",
    "clsx": "^2.1.0",
    "lucide-react": "^0.460.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0",
    "tailwind-merge": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "~5.5.0",
    "vite": "^5.4.0"
  }
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": false,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/tsconfig.node.json`**

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/vite.config.ts`**

```typescript
import path from "node:path";
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    host: "0.0.0.0",
    port: 5173,
    watch: {
      // Required for HMR over Docker bind-mount (Pitfall 4 / Topic 10)
      usePolling: true,
    },
  },
});
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/tailwind.config.js`** (Tailwind v3 syntax — NOT v4)

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ["class"],
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: { "2xl": "1400px" },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/postcss.config.js`**

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/components.json`** (shadcn config — Q4: baseColor=zinc)

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "src/index.css",
    "baseColor": "zinc",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

After files are written, generate `package-lock.json` by running `npm install` from inside the Docker context (do NOT run on host — Pitfall 5 wrong-arch binaries):

```bash
cd C:/Users/om.mengshetti/Desktop/tracer-ai
docker run --rm -v "$(pwd)/frontend":/app -w /app node:20.18.0-alpine npm install
# This generates frontend/package-lock.json AND frontend/node_modules/
# .gitignore already excludes node_modules/; package-lock.json is committed.
```
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      test -f frontend/package.json && \
      test -f frontend/package-lock.json && \
      test -f frontend/tsconfig.json && \
      test -f frontend/tsconfig.node.json && \
      test -f frontend/vite.config.ts && \
      test -f frontend/tailwind.config.js && \
      test -f frontend/postcss.config.js && \
      test -f frontend/components.json && \
      grep -c '"react": "\^18\.3\.1"' frontend/package.json | grep -q '^1$' && \
      grep -c '"tailwindcss": "\^3\.4' frontend/package.json | grep -q '^1$' && \
      grep -c '"@tremor/react": "\^3' frontend/package.json | grep -q '^1$' && \
      grep -c '"@tanstack/react-query": "\^5' frontend/package.json | grep -q '^1$' && \
      grep -c '"react-router-dom": "\^6' frontend/package.json | grep -q '^1$' && \
      ! grep -E '"react": "\^19' frontend/package.json && \
      ! grep -E '"tailwindcss": "\^4' frontend/package.json && \
      grep -c '"baseColor": "zinc"' frontend/components.json | grep -q '^1$' && \
      grep -c 'usePolling: true' frontend/vite.config.ts | grep -q '^1$' && \
      grep -c '"@/\*"' frontend/tsconfig.json | grep -q '^1$' && \
      grep -c 'path.resolve(__dirname, "\./src")' frontend/vite.config.ts | grep -q '^1$'
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 frontend config files exist + package-lock.json.
    - `grep -c '"react": "\^18\.3\.1"' frontend/package.json` returns 1 (NOT React 19).
    - `grep -c '"tailwindcss": "\^3\.4' frontend/package.json` returns 1 (NOT v4).
    - `grep -cE '"react": "\^19' frontend/package.json` returns 0.
    - `grep -cE '"tailwindcss": "\^4' frontend/package.json` returns 0.
    - `grep -c '"baseColor": "zinc"' frontend/components.json` returns 1 (Q4 resolution).
    - `grep -c 'usePolling: true' frontend/vite.config.ts` returns 1 (Pitfall 4 mitigation).
    - `grep -c '"@/\*"' frontend/tsconfig.json` returns 1 (path alias declared).
    - `grep -c 'path.resolve(__dirname, "\./src")' frontend/vite.config.ts` returns 1 (path alias mirrored in Vite).
    - `frontend/package-lock.json` exists.
  </acceptance_criteria>
  <done>
    All Vite + Tailwind + shadcn config files in place; package-lock.json committed; tsc cannot yet run (no source files); Task 2 ships source.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author frontend source — index.html, main.tsx, App.tsx, index.css, lib/utils.ts, components/ui/Card.tsx + Button.tsx</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-CONTEXT.md §"Frontend Skeleton Scope" D-2.29 (vite + react-ts + tailwind + shadcn init + one / route showing Card "Hello tracer-ai")
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-RESEARCH.md §"Topic 7: shadcn CLI 3.x init" (App.tsx canonical at lines 1011-1029)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/docs/wireframes/README.md (component-inventory contract — Card and Button are the only Phase 2 shadcn primitives needed)
  </read_first>
  <action>

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>tracer-ai</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/main.tsx`**

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/App.tsx`** (per D-2.29 + D-2.31)

```tsx
import { Button } from "@/components/ui/button";
import {
  Card,
  CardContent,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function App() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center p-8">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle>Hello tracer-ai</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground mb-4">
            Phase 2 skeleton — RAG features land in Phase 3.
          </p>
          <Button onClick={() => console.log("phase 2 alive")}>Test</Button>
        </CardContent>
      </Card>
    </div>
  );
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/index.css`** (Tailwind v3 directives + shadcn CSS vars for `zinc` base color)

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 240 10% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 240 10% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 240 10% 3.9%;
    --primary: 240 5.9% 10%;
    --primary-foreground: 0 0% 98%;
    --secondary: 240 4.8% 95.9%;
    --secondary-foreground: 240 5.9% 10%;
    --muted: 240 4.8% 95.9%;
    --muted-foreground: 240 3.8% 46.1%;
    --accent: 240 4.8% 95.9%;
    --accent-foreground: 240 5.9% 10%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 240 5.9% 90%;
    --input: 240 5.9% 90%;
    --ring: 240 5.9% 10%;
    --radius: 0.5rem;
  }
  .dark {
    --background: 240 10% 3.9%;
    --foreground: 0 0% 98%;
    --card: 240 10% 3.9%;
    --card-foreground: 0 0% 98%;
    --popover: 240 10% 3.9%;
    --popover-foreground: 0 0% 98%;
    --primary: 0 0% 98%;
    --primary-foreground: 240 5.9% 10%;
    --secondary: 240 3.7% 15.9%;
    --secondary-foreground: 0 0% 98%;
    --muted: 240 3.7% 15.9%;
    --muted-foreground: 240 5% 64.9%;
    --accent: 240 3.7% 15.9%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 0 0% 98%;
    --border: 240 3.7% 15.9%;
    --input: 240 3.7% 15.9%;
    --ring: 240 4.9% 83.9%;
  }
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/lib/utils.ts`** (cn() helper — copied from shadcn standard)

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/components/ui/button.tsx`** (shadcn Button — copied from latest CLI output)

```tsx
import * as React from "react";
import { cn } from "@/lib/utils";

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link";
  size?: "default" | "sm" | "lg" | "icon";
}

const baseClasses =
  "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50";

const variantClasses: Record<NonNullable<ButtonProps["variant"]>, string> = {
  default: "bg-primary text-primary-foreground hover:bg-primary/90",
  destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
  outline: "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
  secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
  ghost: "hover:bg-accent hover:text-accent-foreground",
  link: "text-primary underline-offset-4 hover:underline",
};

const sizeClasses: Record<NonNullable<ButtonProps["size"]>, string> = {
  default: "h-10 px-4 py-2",
  sm: "h-9 rounded-md px-3",
  lg: "h-11 rounded-md px-8",
  icon: "h-10 w-10",
};

export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant = "default", size = "default", ...props }, ref) => {
    return (
      <button
        className={cn(baseClasses, variantClasses[variant], sizeClasses[size], className)}
        ref={ref}
        {...props}
      />
    );
  },
);
Button.displayName = "Button";
```

**File: `C:/Users/om.mengshetti/Desktop/tracer-ai/frontend/src/components/ui/card.tsx`** (shadcn Card — standard scaffold)

```tsx
import * as React from "react";
import { cn } from "@/lib/utils";

export const Card = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      "rounded-lg border bg-card text-card-foreground shadow-sm",
      className,
    )}
    {...props}
  />
));
Card.displayName = "Card";

export const CardHeader = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn("flex flex-col space-y-1.5 p-6", className)}
    {...props}
  />
));
CardHeader.displayName = "CardHeader";

export const CardTitle = React.forwardRef<
  HTMLParagraphElement,
  React.HTMLAttributes<HTMLHeadingElement>
>(({ className, ...props }, ref) => (
  <h3
    ref={ref}
    className={cn("text-2xl font-semibold leading-none tracking-tight", className)}
    {...props}
  />
));
CardTitle.displayName = "CardTitle";

export const CardContent = React.forwardRef<
  HTMLDivElement,
  React.HTMLAttributes<HTMLDivElement>
>(({ className, ...props }, ref) => (
  <div ref={ref} className={cn("p-6 pt-0", className)} {...props} />
));
CardContent.displayName = "CardContent";
```

After files are in place, run `tsc --noEmit` from inside Docker to verify compile cleanliness:

```bash
cd C:/Users/om.mengshetti/Desktop/tracer-ai
docker run --rm -v "$(pwd)/frontend":/app -w /app node:20.18.0-alpine sh -c "npx tsc --noEmit -p tsconfig.json"
# Should exit 0
```
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      test -f frontend/index.html && \
      test -f frontend/src/main.tsx && \
      test -f frontend/src/App.tsx && \
      test -f frontend/src/index.css && \
      test -f frontend/src/lib/utils.ts && \
      test -f frontend/src/components/ui/card.tsx && \
      test -f frontend/src/components/ui/button.tsx && \
      grep -c 'Hello tracer-ai' frontend/src/App.tsx | grep -q '^1$' && \
      grep -c 'from "@/components/ui/card"' frontend/src/App.tsx | grep -q '^1$' && \
      grep -c 'from "@/components/ui/button"' frontend/src/App.tsx | grep -q '^1$' && \
      grep -c '@tailwind base' frontend/src/index.css | grep -q '^1$' && \
      grep -c '@tailwind components' frontend/src/index.css | grep -q '^1$' && \
      grep -c '@tailwind utilities' frontend/src/index.css | grep -q '^1$' && \
      grep -c 'twMerge' frontend/src/lib/utils.ts | grep -q '^1$' && \
      grep -c 'export const Card' frontend/src/components/ui/card.tsx | grep -q '^1$' && \
      grep -c 'export const Button' frontend/src/components/ui/button.tsx | grep -q '^1$' && \
      docker run --rm -v "$(pwd)/frontend":/app -w /app node:20.18.0-alpine sh -c "npx tsc --noEmit -p tsconfig.json"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 source files exist at the listed absolute paths.
    - `grep -c 'Hello tracer-ai' frontend/src/App.tsx` returns 1.
    - `grep -c 'from "@/components/ui/card"' frontend/src/App.tsx` returns 1 (uses path alias correctly).
    - `frontend/src/index.css` contains 3 `@tailwind` directives (base, components, utilities) — Tailwind v3 syntax.
    - `frontend/src/lib/utils.ts` contains `twMerge` and `clsx` imports.
    - `docker run ... node:20-alpine sh -c "npx tsc --noEmit -p tsconfig.json"` exits 0 — TypeScript compiles cleanly across the @/* alias boundary.
  </acceptance_criteria>
  <done>
    Frontend source committed; tsc clean; Wave 5 pre-commit `tsc-frontend` hook has a real lint target.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove Wave 2 web placeholder; verify end-to-end Compose stack with web</name>
  <read_first>
    - C:/Users/om.mengshetti/Desktop/tracer-ai/infra/docker-compose.yml (Wave 2-4 output — find web service block; placeholder is `command: ["sleep", "infinity"]`)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/infra/Dockerfile.frontend (Wave 2 output — dev stage CMD is `["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]`)
    - C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-04-PLAN.md (Wave 4 output — confirms api stack works green so we can additively bring up web)
  </read_first>
  <action>
**Edit `C:/Users/om.mengshetti/Desktop/tracer-ai/infra/docker-compose.yml`**

In the `web:` service block, REMOVE the `command: ["sleep", "infinity"]` line entirely. The `Dockerfile.frontend` `dev` stage already has the canonical `CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]`.

The web service block in compose should now look like:

```yaml
  web:
    build:
      context: ..
      dockerfile: infra/Dockerfile.frontend
      target: dev
    env_file: ../frontend/.env
    ports: ["5173:5173"]
    volumes:
      - ../frontend/src:/app/src
      - ../frontend/index.html:/app/index.html
      - ../frontend/vite.config.ts:/app/vite.config.ts:ro
      - ../frontend/tailwind.config.js:/app/tailwind.config.js:ro
      - ../frontend/postcss.config.js:/app/postcss.config.js:ro
      - ../frontend/tsconfig.json:/app/tsconfig.json:ro
      - ../frontend/tsconfig.node.json:/app/tsconfig.node.json:ro
      - ../frontend/components.json:/app/components.json:ro
      - /app/node_modules  # anonymous volume (Pitfall 5)
    environment:
      CHOKIDAR_USEPOLLING: "true"
    # NO `command:` override here — Dockerfile.frontend dev stage CMD runs npm run dev
```

The expanded volume list bind-mounts every config file the dev server needs (vite.config.ts, tailwind.config.js, postcss.config.js, tsconfig.*, components.json) so editing them on host triggers Vite HMR. The `:ro` suffix on configs prevents accidental in-container modification.

End-to-end verification (closes INFRA-02):

```bash
cd C:/Users/om.mengshetti/Desktop/tracer-ai
cp -f .env.example .env
cp -f frontend/.env.example frontend/.env
cd infra
docker compose down -v --remove-orphans
docker compose up -d --build db migrate api web
# Wait for /healthz
timeout 180 sh -c 'until curl --fail -s http://localhost:8000/healthz > /dev/null 2>&1; do sleep 2; done'
curl -s http://localhost:8000/healthz | jq -e '.status == "ok"'

# Wait for vite dev server (no healthcheck per D-2.34, so probe http://localhost:5173/)
timeout 120 sh -c 'until curl --fail -s http://localhost:5173/ > /tmp/web.html 2>/dev/null; do sleep 2; done'
grep -q '<div id="root"></div>' /tmp/web.html
# The hello-world Card text is rendered client-side, so curl returns the HTML shell.
# Visual confirmation (operator opens browser to http://localhost:5173/) shows the Card.

docker compose down
```
  </action>
  <verify>
    <automated>
      cd C:/Users/om.mengshetti/Desktop/tracer-ai && \
      ! grep -A 30 '^  web:' infra/docker-compose.yml | grep -q 'command: \["sleep' && \
      grep -A 30 '^  web:' infra/docker-compose.yml | grep -q 'CHOKIDAR_USEPOLLING' && \
      grep -A 30 '^  web:' infra/docker-compose.yml | grep -q '/app/node_modules' && \
      cp -f .env.example .env && \
      cp -f frontend/.env.example frontend/.env && \
      cd infra && \
      docker compose down -v --remove-orphans && \
      docker compose up -d --build db migrate api web && \
      timeout 180 sh -c 'until curl --fail -s http://localhost:8000/healthz > /tmp/healthz.json 2>/dev/null; do sleep 2; done' && \
      cat /tmp/healthz.json | jq -e '.status == "ok"' && \
      timeout 120 sh -c 'until curl --fail -s http://localhost:5173/ > /tmp/web.html 2>/dev/null; do sleep 2; done' && \
      grep -q '<div id="root"></div>' /tmp/web.html && \
      curl -s -o /dev/null -w '%{http_code}' http://localhost:5173/ | grep -q '^200$' && \
      docker compose down
    </automated>
  </verify>
  <acceptance_criteria>
    - In `infra/docker-compose.yml`, the `web:` service block does NOT contain `command: ["sleep"`.
    - `web:` service block retains `CHOKIDAR_USEPOLLING` and `/app/node_modules` anonymous volume.
    - `docker compose up -d --build db migrate api web` brings full 4-service stack online from clean slate.
    - `curl http://localhost:8000/healthz` returns 200 + `.status == "ok"` within 180s.
    - `curl http://localhost:5173/` returns 200 + HTML containing `<div id="root"></div>` within 120s after api is healthy.
    - `docker compose down` exits 0.
  </acceptance_criteria>
  <done>
    Full Compose stack boots green from a fresh checkout — db (healthy) + migrate (exited 0) + api (healthy, /healthz=200) + web (serving Vite hello on :5173). INFRA-02 ROADMAP success criterion 1 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vite dev server | Dev-only HTTP; risk is XSS if the hello page rendered untrusted content (Phase 2 hardcoded text only) |
| Browser → API (via VITE_API_BASE_URL) | CORS not opened in Phase 2; Phase 3 adds CORSMiddleware |
| frontend/.env → bundle | VITE_* vars baked into client bundle; risk is committing real secrets to .env (mitigated: only .env.example committed; .env gitignored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05-01 | Tampering | Vite HMR exposes `/__vite_*` debug endpoints to host network | accept | Phase 2 dev only (compose `target: dev`); production `prod` stage uses nginx with no HMR. README in Wave 5 will state "do not bind ports to public interfaces". |
| T-2-05-02 | Tampering | npm install on host with darwin-arm64 binaries bind-mounts incompatible node_modules into linux/amd64 container (Pitfall 5) | mitigate | Dockerfile.frontend uses anonymous `/app/node_modules` volume (Wave 2 acceptance); Task 1 mandates `docker run ... npm install` (NOT host npm install) to generate package-lock.json. Acceptance criterion verifies anonymous volume is in compose. |
| T-2-05-03 | Information Disclosure | VITE_API_BASE_URL committed in `.env.example` could leak if changed to a real internal URL | accept | Default value is `http://localhost:8000` — clearly local. Operator's responsibility to keep production URLs out of `.env.example`. README in Wave 5 documents this. |
| T-2-05-04 | Tampering | Tailwind v4 silently introduced via `npm install --save tailwindcss@latest` | mitigate | `package.json` pins `"tailwindcss": "^3.4.0"`; package-lock.json is committed; pre-commit hook in Wave 5 reads package.json and asserts Tailwind v3 pin via grep. Acceptance criterion `! grep -E '"tailwindcss": "\^4'` enforces. |
| T-2-05-05 | Tampering | React 19 silently introduced same as above | mitigate | Same as T-2-05-04 — pinned to `^18.3.1`; pre-commit grep blocks `"^19`. |
| T-2-05-06 | Spoofing | A future plan adds a different shadcn base color or alias structure | accept | components.json + tsconfig.json paths + vite.config.ts alias are all committed and version-controlled; reviewers see drift in PR. Wave 5 doesn't add a CI gate for this (low priority). |
| T-2-05-07 | Information Disclosure | Browser console logs `console.log("phase 2 alive")` from the Test button — could leak in Phase 3+ if real data flows through | accept | Phase 2 stub Button has hardcoded console.log; Phase 3 wireframes replace this with real handlers. STATE.md will record this as a known stub. |
| T-2-05-08 | Tampering | Compose volume mounts include the entire host frontend/ directory, allowing Vite to serve any host file | mitigate | Compose mount is restricted to `../frontend/src:/app/src` + specific config files (NOT the full `../frontend:/app`). Acceptance criterion verifies the mount list does NOT include `../frontend:/app` blanket mount. |

ASVS Level 1 mapping: V14 Configuration (T-2-05-04, T-2-05-05, T-2-05-08), V8 Data Protection (T-2-05-03). Mitigations are concrete and verified by acceptance criteria. T-2-05-01, T-2-05-03, T-2-05-06, T-2-05-07 accepted with documented downstream-phase ownership.
</threat_model>

<verification>
After all 3 tasks:

```bash
cd C:/Users/om.mengshetti/Desktop/tracer-ai

# 1. All Wave 5 frontend artifacts exist
test -f frontend/package.json
test -f frontend/package-lock.json
test -f frontend/tsconfig.json
test -f frontend/tsconfig.node.json
test -f frontend/vite.config.ts
test -f frontend/tailwind.config.js
test -f frontend/postcss.config.js
test -f frontend/components.json
test -f frontend/index.html
test -f frontend/src/main.tsx
test -f frontend/src/App.tsx
test -f frontend/src/index.css
test -f frontend/src/lib/utils.ts
test -f frontend/src/components/ui/card.tsx
test -f frontend/src/components/ui/button.tsx

# 2. Pinned versions correct (no v4/v19)
grep -E '"react": "\^19' frontend/package.json | wc -l           # → 0
grep -E '"tailwindcss": "\^4' frontend/package.json | wc -l      # → 0
grep -E '"react": "\^18\.3\.1"' frontend/package.json | wc -l    # → 1
grep -E '"tailwindcss": "\^3\.4' frontend/package.json | wc -l   # → 1

# 3. shadcn config has zinc base color
grep -c '"baseColor": "zinc"' frontend/components.json   # → 1

# 4. Path alias mirrored in tsconfig + vite
grep -c '"@/\*"' frontend/tsconfig.json                          # → 1
grep -c 'path.resolve(__dirname, "\./src")' frontend/vite.config.ts  # → 1

# 5. tsc --noEmit clean
docker run --rm -v "$(pwd)/frontend":/app -w /app node:20.18.0-alpine sh -c "npx tsc --noEmit -p tsconfig.json"
# → exit 0

# 6. End-to-end stack: ALL services up; both health probes succeed
cp -f .env.example .env
cp -f frontend/.env.example frontend/.env
cd infra
docker compose down -v --remove-orphans
docker compose up -d --build db migrate api web
timeout 180 sh -c 'until curl --fail -s http://localhost:8000/healthz; do sleep 2; done'
curl -s http://localhost:8000/healthz | jq -e '.status == "ok" and .db == "ok"'
timeout 120 sh -c 'until curl --fail -s http://localhost:5173/; do sleep 2; done'
curl -s http://localhost:5173/ | grep -q '<div id="root"></div>'

# 7. Compose web service no longer has placeholder
grep -A 30 '^  web:' infra/docker-compose.yml | grep -c 'command: \["sleep'  # → 0

docker compose down
```
</verification>

<success_criteria>
- All 15 frontend files committed (8 config + 7 source).
- React pinned to 18.3.1 (NOT 19); Tailwind pinned to 3.4 (NOT 4); shadcn baseColor = zinc.
- Path alias `@/*` mirrored in BOTH `tsconfig.json` paths AND `vite.config.ts` resolve.alias.
- `tsc --noEmit -p frontend/tsconfig.json` exits 0.
- Compose `web` service no longer has `["sleep", "infinity"]` placeholder; bind-mount + anonymous node_modules + CHOKIDAR_USEPOLLING all set per Wave 2 + this plan.
- Full 4-service stack boots from fresh `docker compose up`: db (healthy), migrate (exited 0), api (healthy, /healthz=200), web (serving on 5173).
- INFRA-02 fully satisfied: `curl localhost:8000/healthz` → ok; `curl localhost:5173/` → 200 + HTML shell with `<div id="root">`.
- Wave 5 pre-commit plan can run `tsc-frontend` hook with a real lint target.
</success_criteria>

<output>
After completion, create `C:/Users/om.mengshetti/Desktop/tracer-ai/.planning/phases/02-skeleton-infrastructure/02-05-SUMMARY.md` per the GSD summary template, including:
- Frontend dep manifest table (every dep with pinned version)
- Open Question Q4 closed: baseColor = zinc
- Tailwind v3 + React 18 pin enforcement (negative grep gates)
- tsc --noEmit exit 0 confirmation
- INFRA-02 closure: stack 4-service boot from fresh checkout
- Stub note: console.log in Button is a Phase 3 wireframe replacement target
- Wave 5 readiness: pre-commit hooks have real Python + TypeScript targets
</output>
