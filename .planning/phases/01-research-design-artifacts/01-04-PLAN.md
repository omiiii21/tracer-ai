---
phase: 01-research-design-artifacts
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/trace-schema.md
autonomous: true
requirements: [DSGN-04]
tags: [trace, schema, otel, design]

must_haves:
  truths:
    - "/docs/trace-schema.md exists with attribute constants block + 6 span sections"
    - "Constants block contains all gen_ai.* and rag.* attribute names verbatim per D-21"
    - "gen_ai.system is explicitly marked DEPRECATED; gen_ai.provider.name is the live name (D-22)"
    - "Payload Storage Convention section warns AGAINST storing full prompts/responses as span attrs (D-47)"
    - "6 span sections: rag.request, rag.retrieve, rag.prompt_assemble, rag.llm_call, rag.eval, feedback.user (D-19)"
    - "Each span section has one-line purpose, attribute table, JSON example, payload-table reference"
  artifacts:
    - path: "docs/trace-schema.md"
      provides: "Trace schema spec — every span name, attribute, type, OTel status, example payload (DSGN-04)"
      contains: "rag.request"
  key_links:
    - from: "docs/trace-schema.md"
      to: "Phase 4 tracer_ai/tracer/span.py (TRCR-01)"
      via: "Python constants block (copy-paste-ready)"
      pattern: "GEN_AI_PROVIDER_NAME"
    - from: "docs/trace-schema.md"
      to: "docs/decisions/005-observability-strategy.md"
      via: "ADR cross-reference"
      pattern: "005-observability-strategy"
---

<objective>
Author the trace schema specification at `docs/trace-schema.md`. Every span name, every attribute, every type, every example payload. The Python constants block is copy-paste-ready into `tracer_ai/tracer/span.py` in Phase 4 TRCR-01 — it IS the contract.

Purpose: Resolve DSGN-04. The fresh-agent docs check Q3 ("what is the trace schema — list spans, key attributes, where payloads are stored") is answered EXCLUSIVELY by this file. Without it, Phase 4 TRCR-01 has to re-derive every attribute name from research.

Output: Single ~250-350 line markdown file with structured span-by-span spec.
</objective>

<execution_context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-design-artifacts/01-CONTEXT.md
@.planning/phases/01-research-design-artifacts/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@docs/decisions/005-observability-strategy.md

<wave_2_dependency_note>
This plan runs in Wave 2 — Wave 1 must have created `docs/decisions/005-observability-strategy.md`. The trace schema cites that ADR for OTel naming rationale. The schema constants here are the source-of-truth that ADR 005 promises to import in Phase 4 TRCR-01.
</wave_2_dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/trace-schema.md (DSGN-04, full spec with 6 spans + Python constants block)</name>
  <files>docs/trace-schema.md</files>
  <read_first>
    - .planning/phases/01-research-design-artifacts/01-CONTEXT.md (D-19..D-22, D-47)
    - .planning/phases/01-research-design-artifacts/01-RESEARCH.md §"Per-Artifact Authoring Guide › Artifact 5" (lines ~243-300; includes the verbatim Python constants block ~258-287 and per-span attribute draft table ~290-298)
    - .planning/research/ARCHITECTURE.md §"OTel GenAI Semantic Conventions — Status as of 2026" + §"Anti-Patterns" (Pitfall #2 / #6)
    - docs/decisions/005-observability-strategy.md (Wave 1 — cite for OTel rationale)
  </read_first>
  <action>
Create `docs/trace-schema.md` with this exact structure. Length target ~250-350 LOC.

**1. `# Trace Schema Specification` (h1).**

**2. `## Overview`** — one paragraph: "Every chat request produces one trace with 4 child spans (`rag.retrieve`, `rag.prompt_assemble`, `rag.llm_call`, plus async `rag.eval`). Spans use OTel GenAI attribute names where defined, custom `rag.*` namespace for RAG-specific attributes. ALL `gen_ai.*` are Development/Experimental stability — naming may change; we centralize names as constants in `tracer_ai/tracer/span.py` (Phase 4 TRCR-01) so a future spec revision is a one-file edit. See [ADR 005](./decisions/005-observability-strategy.md) for rationale."

**3. `## OTel Status Disclaimer`** — explicit D-22 note. Required content: "The OTel GenAI semantic conventions are at **Development/Experimental** stability as of May 2026. Two facts to encode:
- **`gen_ai.system` is DEPRECATED**. Do not use it. Use `gen_ai.provider.name` instead. For tracer-ai, `gen_ai.provider.name = \"anthropic\"`.
- The full `gen_ai.*` set may rename in future spec revisions. Mitigation: every attribute name is a Python constant in one file (see §Attribute Constants below); a spec rename is a one-line edit per constant."

**4. `## Attribute Constants`** — fenced Python block (` ```python `), copy-paste-ready into `tracer_ai/tracer/span.py`. Use the verbatim block from 01-RESEARCH.md lines ~258-287:

```python
# OTel GenAI conventions (Development stability; gen_ai.system DEPRECATED)
GEN_AI_OPERATION_NAME = "gen_ai.operation.name"   # "chat" | "embeddings" | "retrieval"
GEN_AI_PROVIDER_NAME = "gen_ai.provider.name"     # "anthropic" — USE THIS
# GEN_AI_SYSTEM = "gen_ai.system"                 # DEPRECATED; do not use
GEN_AI_REQUEST_MODEL = "gen_ai.request.model"
GEN_AI_RESPONSE_MODEL = "gen_ai.response.model"
GEN_AI_USAGE_INPUT_TOKENS = "gen_ai.usage.input_tokens"
GEN_AI_USAGE_OUTPUT_TOKENS = "gen_ai.usage.output_tokens"
GEN_AI_RETRIEVAL_QUERY_TEXT = "gen_ai.retrieval.query.text"
GEN_AI_RETRIEVAL_DOCUMENTS = "gen_ai.retrieval.documents"

# Custom rag.* namespace (no official rag.* exists in OTel)
RAG_RETRIEVED_CHUNK_IDS = "rag.retrieved_chunk_ids"
RAG_RETRIEVAL_TOP_K = "rag.retrieval.top_k"
RAG_RETRIEVAL_SCORE_MEAN = "rag.retrieval.score.mean"
RAG_RETRIEVAL_SCORE_MIN = "rag.retrieval.score.min"
RAG_RETRIEVAL_SCORE_MAX = "rag.retrieval.score.max"
RAG_PROMPT_TEMPLATE_ID = "rag.prompt_template.id"
RAG_PROMPT_TOKEN_COUNT = "rag.prompt.token_count"
RAG_EMBEDDING_MODEL = "rag.embedding.model"
RAG_EMBEDDING_MODEL_VERSION = "rag.embedding.model_version"
RAG_EVAL_FAITHFULNESS = "rag.eval.faithfulness"
RAG_EVAL_RELEVANCE = "rag.eval.relevance"
RAG_EVAL_JUDGE_MODEL = "rag.eval.judge_model"
RAG_EVAL_JUDGE_PROMPT_VERSION = "rag.eval.judge_prompt_version"
RAG_EVAL_JUDGE_COST_USD = "rag.eval.judge_cost_usd"
RAG_EVAL_JUDGE_LATENCY_MS = "rag.eval.judge_latency_ms"
```

Add a one-line note below the block: "These constants are imported into `tracer_ai/tracer/span.py` in Phase 4 TRCR-01."

**5. `## Payload Storage Convention`** — explicit D-47 anti-pattern warning. Required content (use a `> **Warning**` callout block):

> **Warning** — full prompt/response text **must NOT** be stored as span attributes. The OTel attribute size limit is 4-16KB per attribute; assembled prompts and LLM responses routinely exceed this. Storing them on the span row also bloats GIN indexes and slows trace listing. **Use the `span_payloads` JSONB side table** (see [Data Model](./data-model.md)) referenced by `span_id`. Span attribute rows hold only metadata: scores, IDs, token counts, model names, etc.

**6. Six `##` per-span sections, in this exact order (D-19):**

For each span, the structure is:
- `## <span_name>` (h2)
- One-line **Purpose** sentence.
- **Attribute table** with columns: `name | type | required | OTel status | example`. (Use markdown tables; no newlines inside cells — Pitfall D in 01-RESEARCH.md.)
- **JSON example payload** in a fenced ` ```json ` block.
- **Payload table reference** (where the heavy content lives, if any).

The 6 spans + their content (use the per-span draft table from 01-RESEARCH.md lines ~290-298 as the source-of-truth):

**6.1 `## rag.request` (root span)**
- Purpose: Root of every chat request trace; one per HTTP `POST /chat` invocation.
- Required attributes:
  - `gen_ai.operation.name` | string | yes | OTel GenAI Development | `"chat"`
  - `gen_ai.provider.name` | string | yes | OTel GenAI Development | `"anthropic"`
  - `query_text` | string | yes | custom | `"How do I authenticate to the Anthropic Messages API?"`
- Optional:
  - `feedback.rating` | int (-1, 0, 1) | no (post-feedback) | custom | `1`
  - `feedback.comment_id` | UUID | no | custom | `"550e8400-..."`
- JSON example: an object with `name: "rag.request"`, `trace_id`, `span_id`, `started_at`, `ended_at`, `attrs: {...}`.
- Payload table: none (root has no heavy payload).

**6.2 `## rag.retrieve`**
- Purpose: Vector retrieval step — embed the query and pull top_k chunks from pgvector.
- Required attributes:
  - `gen_ai.operation.name` | string | yes | OTel GenAI Development | `"retrieval"`
  - `rag.embedding.model` | string | yes | custom | `"voyage-code-3"`
  - `rag.retrieval.top_k` | int | yes | custom | `5`
  - `rag.retrieval.score.mean` | float | yes | custom | `0.78`
  - `rag.retrieval.score.min` | float | yes | custom | `0.62`
  - `rag.retrieval.score.max` | float | yes | custom | `0.91`
  - `rag.retrieved_chunk_ids` | list[UUID] | yes | custom | `["uuid1", "uuid2", ...]`
- Optional: `gen_ai.retrieval.query.text` (Opt-In per OTel; only when query-text logging is enabled) | string | no | OTel GenAI Opt-In | `"How do I auth..."`
- JSON example payload.
- Payload table: full chunk text + per-chunk scores -> `span_payloads` keyed by `span_id`.

**6.3 `## rag.prompt_assemble`**
- Purpose: Assemble the final LLM prompt from retrieved chunks + user query + system instruction.
- Required attributes:
  - `rag.prompt_template.id` | string | yes | custom | `"v1.basic-citation"`
  - `rag.prompt.token_count` | int | yes | custom | `1247`
- JSON example.
- Payload table: full assembled prompt text -> `span_payloads`.

**6.4 `## rag.llm_call`**
- Purpose: Call Anthropic Messages API with the assembled prompt.
- Required attributes:
  - `gen_ai.operation.name` | string | yes | OTel GenAI Development | `"chat"`
  - `gen_ai.provider.name` | string | yes | OTel GenAI Development | `"anthropic"`
  - `gen_ai.request.model` | string | yes | OTel GenAI Development | `"claude-sonnet-4-5-20250929"` (dated snapshot — Pitfall #4)
  - `gen_ai.response.model` | string | yes | OTel GenAI Development | `"claude-sonnet-4-5-20250929"`
  - `gen_ai.usage.input_tokens` | int | yes | OTel GenAI Development | `1247`
  - `gen_ai.usage.output_tokens` | int | yes | OTel GenAI Development | `412`
- Optional: `gen_ai.response.finish_reasons` | list[str] | no | OTel GenAI | `["end_turn"]`
- JSON example.
- Payload table: full response text -> `span_payloads`.

**6.5 `## rag.eval` (async, child of `rag.request` via context snapshot)**
- Purpose: LLM-as-judge scores faithfulness + relevance. Runs in a `BackgroundTasks` async branch AFTER response flush. Uses the OTel context snapshot captured before `root.end()` (Pitfall #1) to attach as a child of `rag.request` rather than orphaning as a new root.
- Required attributes:
  - `rag.eval.faithfulness` | float (0.0-1.0) | yes | custom | `0.82`
  - `rag.eval.relevance` | float (0.0-1.0) | yes | custom | `0.91`
  - `rag.eval.judge_model` | string | yes | custom | `"claude-haiku-4-5-20251001"` (**dated snapshot mandatory** — D-50, Pitfall #4)
  - `rag.eval.judge_prompt_version` | string | yes | custom | `"v1.ragas-faithfulness"`
  - `rag.eval.judge_cost_usd` | float | yes | custom | `0.00012`
- Optional: `rag.eval.judge_latency_ms` | int | no | custom | `850`
- JSON example.
- Payload table: full judge prompt + judge response -> `span_payloads`.

**6.6 `## feedback.user` (event-style, not a timed span)**
- Purpose: Recorded when the user clicks thumbs-up / thumbs-down. Not a duration span — emit as an event correlated to the trace via `feedback.trace_id`.
- Required attributes:
  - `feedback.rating` | int (1 = up, -1 = down) | yes | custom | `-1`
  - `feedback.trace_id` | UUID | yes | custom | `"550e8400-..."`
- Optional: `feedback.comment` | string | no | custom | `"Wrong chunks retrieved"`. `feedback.diagnosis_tag` | string | no (Phase 5 FBCK-05 future) | custom | `"Retrieval"`.
- JSON example.
- Payload table: none.

**7. `## Cross-References` (final section)** — relative links: `[Architecture](./architecture.md)`, `[Sequence Diagrams](./sequence-diagrams.md)`, `[Data Model](./data-model.md)`, `[ADR 005 — Observability Strategy](./decisions/005-observability-strategy.md)`.
  </action>
  <verify>
    <automated>test -f docs/trace-schema.md && grep -q '^# Trace Schema Specification' docs/trace-schema.md && grep -q 'GEN_AI_PROVIDER_NAME = "gen_ai.provider.name"' docs/trace-schema.md && grep -q 'gen_ai.system' docs/trace-schema.md && grep -q 'DEPRECATED' docs/trace-schema.md && grep -q 'span_payloads' docs/trace-schema.md && grep -q 'rag.eval.faithfulness' docs/trace-schema.md && grep -q 'rag.eval.relevance' docs/trace-schema.md && grep -q 'rag.retrieval.score.mean' docs/trace-schema.md && grep -q 'rag.prompt_template.id' docs/trace-schema.md && for s in 'rag.request' 'rag.retrieve' 'rag.prompt_assemble' 'rag.llm_call' 'rag.eval' 'feedback.user'; do grep -q "^## $s" docs/trace-schema.md || { echo "MISSING SPAN SECTION: $s"; exit 1; }; done && grep -q 'claude-haiku' docs/trace-schema.md && grep -q '005-observability-strategy' docs/trace-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Contains `# Trace Schema Specification` h1
    - Contains constant `GEN_AI_PROVIDER_NAME = "gen_ai.provider.name"` in a Python block
    - Mentions `gen_ai.system` AND `DEPRECATED` (D-22)
    - Mentions `span_payloads` (D-47 payload-storage convention)
    - Contains `rag.eval.faithfulness` and `rag.eval.relevance` constants
    - Contains `rag.retrieval.score.mean` and `rag.prompt_template.id` constants
    - All 6 span sections present as `## <span_name>` headings: rag.request, rag.retrieve, rag.prompt_assemble, rag.llm_call, rag.eval, feedback.user
    - Mentions `claude-haiku` (judge model, dated snapshot mandate per D-50)
    - Cross-references ADR 005 (`005-observability-strategy`)
  </acceptance_criteria>
  <done>docs/trace-schema.md committed with constants block, OTel deprecation note, payload convention warning, and all 6 span sections fully specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author -> markdown file | Sole boundary |
| Phase 1 -> Phase 4 TRCR-01 | The Python constants block IS the contract |
| Phase 1 -> Phase 4 TRCR-02/03 | The per-span attribute lists ARE the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | Constants block contains `gen_ai.system` (deprecated) as a live name instead of marking it deprecated | mitigate | The block comments out `GEN_AI_SYSTEM` with a `# DEPRECATED; do not use` note; verify step ensures DEPRECATED appears. |
| T-01-04-02 | Tampering | Per-span attribute table missing required attribute (e.g., `rag.eval.judge_model`) | mitigate | Per-span lists are explicit in the action above; verify step greps for canonical attribute names. |
| T-01-04-03 | Information Disclosure | Example JSON payload contains a real query that itself contains a secret | accept | Example queries are user-facing question text; no secrets. |
</threat_model>

<verification>
- File exists, ~250-350 LOC
- Constants block is copy-paste-ready Python
- 6 span sections all present
- D-22 / D-47 / D-50 mandates encoded and grep-verifiable
</verification>

<success_criteria>
- DSGN-04 satisfied
- Phase 4 TRCR-01 can copy the constants block directly into `tracer_ai/tracer/span.py`
- Fresh-agent docs check Q3 ("trace schema — list spans, attributes, where payloads live") answerable from this file alone
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-design-artifacts/01-04-SUMMARY.md` with: file path, line count, the 6 span names listed in order, and confirmation that the constants block is copy-paste-ready (no surrounding markdown to strip).
</output>
