---
phase: 01-research-design-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - docs/module-deps.md
autonomous: true
requirements: [DSGN-02, DSGN-08]
tags: [diagram, mermaid, architecture, design]

must_haves:
  truths:
    - "/docs/architecture.md exists with one Mermaid flowchart TD diagram"
    - "/docs/module-deps.md exists with one Mermaid flowchart LR diagram"
    - "Architecture diagram has subgraphs for Frontend, FastAPI Backend, and Persistence (Postgres 16)"
    - "Architecture diagram references external services Anthropic and Voyage AI"
    - "Module-deps diagram has 8 nodes (config, tracer/, rag/, eval/, corpus/, api/, cli/, errors) and is acyclic"
  artifacts:
    - path: "docs/architecture.md"
      provides: "System architecture diagram with 3-tier subgraphs (DSGN-02)"
      contains: "flowchart TD"
    - path: "docs/module-deps.md"
      provides: "Module dependency diagram (DSGN-08)"
      contains: "flowchart LR"
  key_links:
    - from: "docs/architecture.md"
      to: "docs/sequence-diagrams.md (created in Wave 3)"
      via: "narrative pointer"
      pattern: "sequence-diagrams"
    - from: "docs/architecture.md"
      to: "docs/module-deps.md"
      via: "narrative pointer"
      pattern: "module-deps"
---

<objective>
Author the system architecture diagram and module dependency diagram — the two foundational Mermaid diagrams that describe **what tracer-ai is** (architecture) and **how its modules import** (deps). Both can author entirely from `.planning/research/ARCHITECTURE.md` with no dependencies on other Phase 1 artifacts.

Purpose: Resolve DSGN-02 (architecture diagram) and DSGN-08 (module-deps diagram with no circular deps). The architecture diagram is the load-bearing answer to the fresh-agent's Q1 ("what does the system do") and Q2 ("how does data flow"). The module-deps diagram is the gate for ROADMAP success criterion 4 ("zero circular dependencies").

Output: 2 markdown files, each with one Mermaid block + supporting prose. Pure documentation.
</objective>

<execution_context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-research-design-artifacts/01-CONTEXT.md
@.planning/phases/01-research-design-artifacts/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@CLAUDE.md

<github_mermaid_compat_rule>
Both diagrams must render in GitHub's Mermaid renderer (Pitfall A in 01-RESEARCH.md). Stick to the syntax shown in 01-RESEARCH.md §"Mermaid Syntax Reference". Quote any node label containing `(`, `)`, `[`, `]`, `<`, `>`, or `:`. Do NOT use `defaultRenderer: elk` directives or experimental shapes (`A@{shape: ...}`).
</github_mermaid_compat_rule>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/architecture.md (system architecture, DSGN-02)</name>
  <files>docs/architecture.md</files>
  <read_first>
    - .planning/phases/01-research-design-artifacts/01-CONTEXT.md (D-15 — flowchart TD with subgraphs Frontend / FastAPI / Persistence)
    - .planning/phases/01-research-design-artifacts/01-RESEARCH.md §"Per-Artifact Authoring Guide › Artifact 3" + §"Mermaid Syntax Reference › flowchart" (lines ~417-482; sample diagram at lines ~423-455 is copy-paste-ready)
    - .planning/research/ARCHITECTURE.md §"System Overview" (ASCII source-of-truth — mirror its structure) and §"Component Responsibilities"
    - .planning/research/STACK.md (for accurate component naming — Tremor, shadcn, Tailwind v3, FastAPI, etc.)
  </read_first>
  <action>
Create `docs/architecture.md` with this exact structure:

1. `# System Architecture` (h1).

2. **One-paragraph framing** (~3-4 sentences): "tracer-ai is a three-tier system — a React 18 + Vite SPA (Tailwind v3 + Tremor v3 + shadcn/ui) calls a Python 3.12 FastAPI backend, which queries a single Postgres 16 instance hosting both `pgvector` (chunk embeddings) and the trace database (5 tables, JSONB-with-GIN attribute storage). Every RAG pipeline stage emits a span; spans funnel through an `asyncio.Queue` to a background consumer that batch-writes JSONB to Postgres. External calls go to Anthropic (Sonnet 4.5 for the bot, Haiku for the judge) and Voyage AI (`voyage-code-3` embeddings). The judge runs in a `BackgroundTasks` async branch so eval failures never break user requests."

3. **Mermaid `flowchart TD` block** with three subgraphs and external service nodes. Use the diagram from 01-RESEARCH.md lines ~423-455 verbatim, then verify subgraph IDs and node labels render in GitHub. Required content:
   - Subgraph `fe["Frontend (Vite + React 18 + Tailwind v3 + Tremor v3)"]` containing nodes: `chat[Chat UI]`, `dash[Dashboard]`, `admin[Admin]`.
   - Subgraph `be["FastAPI Backend"]` containing nodes: `api["api/chat.py, api/traces.py, api/feedback.py, api/admin.py"]`, `pipe[rag/pipeline.py]`, `tracer[tracer/]`, `eval[eval/llm_judge.py]`. (Quote `api[...]` label since it contains `,` and `/`.)
   - Subgraph `db["Persistence (Postgres 16)"]` containing nodes: `traces[(traces / spans / span_payloads / feedback / regression_cases)]`, `chunks[(chunks — pgvector HNSW)]`. Use cylinder shape `[(...)]` for both database nodes.
   - External service nodes (stadium shape `([...])`): `anthropic([Anthropic API])`, `voyage([Voyage AI API])`.
   - Edges:
     - `chat -->|HTTP/JSON| api`
     - `dash -->|HTTP/JSON| api`
     - `admin -->|HTTP/JSON| api`
     - `api --> pipe`
     - `api -.async.-> eval`  (dotted = async / out-of-band)
     - `pipe --> tracer`
     - `pipe --> anthropic`
     - `pipe --> voyage`
     - `pipe --> chunks`
     - `tracer --> traces`
     - `eval --> anthropic`
     - `eval --> tracer`

4. **Component Responsibilities table** (mirror `.planning/research/ARCHITECTURE.md §"Component Responsibilities"`, trim to one screen). Markdown table with columns `Module | Path | Responsibility`. Required rows (one each):
   - `Chat UI | frontend/src/routes/chat.tsx | Single-turn or multi-turn chat; renders cited chunks, latency, tokens, cost, thumbs feedback`
   - `Dashboard | frontend/src/routes/dashboard*.tsx | Trace list, trace detail, bad-answer queue, time-series charts`
   - `Admin UI | frontend/src/routes/admin.tsx | Corpus stats, re-index trigger, chunking config, URL ingest`
   - `api/ | tracer_ai/api/ | FastAPI routes; Pydantic request/response schemas; HTTP error envelope`
   - `rag/pipeline.py | tracer_ai/rag/pipeline.py | Orchestrates retrieve -> prompt_assemble -> llm_call; emits spans`
   - `tracer/ | tracer_ai/tracer/ | Span dataclass, context propagation, async queue + Postgres writer`
   - `eval/ | tracer_ai/eval/ | LLM-as-judge worker (Haiku); RAGAS-style faithfulness + relevance prompts`
   - `corpus/ | tracer_ai/corpus/ | Markdown-header chunker, Voyage embedder, pgvector retriever`
   - `chunks (pgvector) | Postgres extension | 1024-dim vectors with HNSW cosine_ops index`
   - `traces / spans / span_payloads / feedback / regression_cases | Postgres tables | Trace database (JSONB attrs, GIN-indexed; spans partitioned by started_at month)`

5. **Cross-reference paragraph at end:** "For the request-time data flow, see [/docs/sequence-diagrams.md](./sequence-diagrams.md). For module-level imports + acyclicity, see [/docs/module-deps.md](./module-deps.md). For the per-span attribute schema, see [/docs/trace-schema.md](./trace-schema.md). Decisions behind the stack live in [/docs/decisions/](./decisions/README.md)."

Length target: ~80-120 lines including the Mermaid block. Quote any node label containing `(`, `,`, or `/` to avoid GitHub Mermaid parser issues (e.g., `api["api/chat.py, api/traces.py, ..."]`).
  </action>
  <verify>
    <automated>test -f docs/architecture.md && grep -q '^# System Architecture' docs/architecture.md && grep -q '^```mermaid' docs/architecture.md && grep -q 'flowchart TD' docs/architecture.md && grep -q 'subgraph fe' docs/architecture.md && grep -q 'subgraph be' docs/architecture.md && grep -q 'subgraph db' docs/architecture.md && grep -q 'anthropic' docs/architecture.md && grep -q 'voyage' docs/architecture.md && grep -q 'pgvector' docs/architecture.md && grep -q 'BackgroundTasks' docs/architecture.md && grep -q 'sequence-diagrams' docs/architecture.md && grep -q 'module-deps' docs/architecture.md && ! grep -q 'defaultRenderer' docs/architecture.md && ! grep -q 'A@{shape' docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Contains `# System Architecture` h1
    - Contains a Mermaid block opening with ` ```mermaid` (literal triple-backtick mermaid)
    - Mermaid block declares `flowchart TD`
    - Three subgraph IDs present: `fe`, `be`, `db`
    - External services `anthropic` and `voyage` named in the diagram
    - "pgvector" mentioned in the diagram or surrounding prose
    - "BackgroundTasks" mentioned in the framing paragraph (async eval branch)
    - File cross-references `/docs/sequence-diagrams.md` and `/docs/module-deps.md`
    - No `defaultRenderer:` config directives or experimental `A@{shape:` syntax (Pitfall A)
  </acceptance_criteria>
  <done>docs/architecture.md committed with one valid Mermaid flowchart TD block, three subgraphs (Frontend / Backend / Persistence), external services named, component responsibilities table, and cross-refs to other Phase 1 artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/module-deps.md (module dependency graph, DSGN-08)</name>
  <files>docs/module-deps.md</files>
  <read_first>
    - .planning/phases/01-research-design-artifacts/01-CONTEXT.md (D-18 — flowchart LR with one node per module: config, tracer/, rag/, eval/, corpus/, api/, cli/, errors)
    - .planning/phases/01-research-design-artifacts/01-RESEARCH.md §"Per-Artifact Authoring Guide › Artifact 8" + §"Mermaid Syntax Reference › flowchart"
    - .planning/research/ARCHITECTURE.md §"Dependency Graph (no cycles)" — the canonical source-of-truth for the imports
  </read_first>
  <action>
Create `docs/module-deps.md` with this exact structure:

1. `# Module Dependency Graph` (h1).

2. **One-paragraph framing**: "Each module imports only from its declared dependencies. The visual acyclicity check is the Phase 1 gate (DSGN-08) — read the diagram below and confirm there are no cycles. The runtime check (e.g., `import-linter` or a pre-commit AST analyzer) is Phase 2 INFRA-04. The arrow direction reads as 'data/dependency flows FROM A TO B' which is also the import direction (`B imports A` means `A --> B`). Module layering matches `.planning/research/ARCHITECTURE.md §'Dependency Graph (no cycles)'`."

3. **Mermaid `flowchart LR` block** with 8 nodes, layered left-to-right. Required content:
   - Nodes: `config[config]`, `errors[errors]`, `tracer[tracer/]`, `rag[rag/]`, `corpus[corpus/]`, `eval[eval/]`, `api[api/]`, `cli[cli/]`.
   - Edges (read as "imports flow FROM source TO target"; this matches the natural reading "tracer/ depends on config"):
     - `config --> tracer`
     - `errors --> tracer`
     - `config --> corpus`
     - `errors --> corpus`
     - `config --> rag`
     - `errors --> rag`
     - `tracer --> rag`
     - `corpus --> rag`
     - `config --> eval`
     - `errors --> eval`
     - `tracer --> eval`
     - `config --> api`
     - `errors --> api`
     - `rag --> api`
     - `eval --> api`
     - `tracer --> api`
     - `config --> cli`
     - `errors --> cli`
     - `rag --> cli`
     - `corpus --> cli`
     - `eval --> cli`
   - The graph is layered: `{config, errors}` are leaves on the left; `tracer, corpus` next; `rag, eval` middle; `api, cli` rightmost (entry points). All edges flow strictly left-to-right — visually confirms acyclicity.

4. **Module Purpose Table** (markdown table with columns `Module | Purpose | Imports From | Imported By`):
   - `config | Pydantic Settings; loads env vars; single source of truth for ANTHROPIC_API_KEY, VOYAGE_API_KEY, embedding_model, chunk_size, etc. | (none — leaf) | tracer/, corpus/, rag/, eval/, api/, cli/`
   - `errors | Cross-cutting error types and the API error envelope shape | (none — leaf) | tracer/, corpus/, rag/, eval/, api/, cli/`
   - `tracer/ | Span dataclass, context propagation, asyncio.Queue + Postgres background writer | config, errors | rag/, eval/, api/`
   - `corpus/ | Markdown-header chunker, Voyage embedder Protocol + adapter, pgvector retriever | config, errors | rag/, cli/`
   - `rag/ | Pipeline orchestrator (retrieve -> prompt_assemble -> llm_call); Anthropic LLM Protocol + adapter | config, errors, tracer/, corpus/ | api/, cli/`
   - `eval/ | LLM-as-judge worker; RAGAS-style faithfulness + relevance prompts; runs via BackgroundTasks | config, errors, tracer/ | api/, cli/`
   - `api/ | FastAPI routes + Pydantic schemas; HTTP-layer error envelope | config, errors, tracer/, rag/, eval/ | (none — entry point) |`
   - `cli/ | Typer-based command surface (tracer-ai ingest, tracer-ai eval, tracer-ai promote) | config, errors, rag/, corpus/, eval/ | (none — entry point) |`

5. **Acyclicity Check** (final section):
   - Heading: `## Acyclicity Check`
   - One paragraph: "Visual inspection: every edge flows strictly left-to-right. `config` and `errors` have zero incoming edges. `api` and `cli` have zero outgoing edges. No node both imports and is imported by the same other node. Therefore no cycles exist. Phase 2 INFRA-04 will install a pre-commit AST analyzer (e.g., `import-linter`) to runtime-enforce this property."

Length target: ~70-100 lines.
  </action>
  <verify>
    <automated>test -f docs/module-deps.md && grep -q '^# Module Dependency Graph' docs/module-deps.md && grep -q '^```mermaid' docs/module-deps.md && grep -q 'flowchart LR' docs/module-deps.md && for n in config errors tracer rag corpus eval api cli; do grep -q "$n" docs/module-deps.md || { echo "MISSING NODE $n"; exit 1; }; done && grep -q 'INFRA-04' docs/module-deps.md && grep -q 'Acyclicity' docs/module-deps.md && ! grep -q 'defaultRenderer' docs/module-deps.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Contains `# Module Dependency Graph` h1
    - Contains a Mermaid `flowchart LR` block
    - All 8 module names appear: config, errors, tracer, rag, corpus, eval, api, cli
    - Module purpose table present
    - References INFRA-04 as the future runtime acyclicity check
    - "Acyclicity" heading or section confirms the visual check
    - No `defaultRenderer:` config directives (Pitfall A)
  </acceptance_criteria>
  <done>docs/module-deps.md committed with one valid Mermaid flowchart LR, 8 module nodes, no cycles (verifiable visually — every edge flows left-to-right), and module purpose table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author -> markdown file | Sole boundary; Mermaid blocks are inert text in the source repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Mermaid syntax that doesn't render in GitHub | mitigate | Author follows verified syntax in 01-RESEARCH.md §"Mermaid Syntax Reference"; verify step greps for forbidden directives (`defaultRenderer`, `A@{shape:`) (Pitfall A). |
| T-01-02-02 | Tampering | Module-deps diagram has cycles | mitigate | Diagram is layered LR; visual acyclicity is the Phase 1 gate. The structural rule "edges only go left-to-right" is enforced by the per-edge list in the action above. |
| T-01-02-03 | Information Disclosure | Architecture diagram leaks a secret | accept | No secrets are referenced in either file; verify-step note confirms no `sk-`/`API_KEY=` patterns. |
</threat_model>

<verification>
- Both files exist
- Each contains exactly one Mermaid block (`flowchart TD` for architecture, `flowchart LR` for module-deps)
- All 8 module nodes present in module-deps
- All 3 subgraphs present in architecture
- Cross-reference links from architecture.md to sequence-diagrams.md and module-deps.md present
</verification>

<success_criteria>
- DSGN-02 (system architecture diagram) satisfied via docs/architecture.md
- DSGN-08 (module-deps with no cycles) satisfied via docs/module-deps.md
- Fresh-agent docs check Wave 4: Q1 ("what does the system do") answerable from architecture.md framing paragraph + diagram + component table
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-design-artifacts/01-02-SUMMARY.md` with: list of files created, line counts, the Mermaid renderer compatibility note (verified GitHub-safe per the syntax rules), and any deviations from the per-artifact specs above.
</output>
