---
phase: 01-research-design-artifacts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/eval/coverage_set.yaml
autonomous: true
requirements: [DSGN-10]
tags: [eval, coverage, yaml, design]

must_haves:
  truths:
    - "/docs/eval/coverage_set.yaml exists with at least 12 query entries"
    - "Each entry has id, query, doc_section, expected_chunk_keywords (list), expected_min_score, notes"
    - "12 doc_section values cover: auth, models, messages, tools, batches, files, citations, vision, errors-and-rate-limits, prompt-caching, agent-sdk-overview, agent-sdk-tools"
  artifacts:
    - path: "docs/eval/coverage_set.yaml"
      provides: "Proactive coverage regression query set (DSGN-10)"
      contains: "COV-01"
  key_links:
    - from: "docs/eval/coverage_set.yaml"
      to: "Phase 6 CLI-02 regression runner"
      via: "yaml.safe_load() consumer"
      pattern: "doc_section"
---

<objective>
Author the proactive coverage regression query set at `docs/eval/coverage_set.yaml` per DSGN-10. The 12 queries are already drafted verbatim in `01-RESEARCH.md §"Coverage Query Set Draft"` — this plan codifies that draft into the canonical YAML file. The file becomes the contract between Phase 1 (authors the queries) and Phase 6 CLI-02 (loads + runs them).

Purpose: Resolve DSGN-10 (proactive coverage query set with 10+ queries covering each major Claude API doc section). Phase 1 authors the file; Phase 5 EVAL-06 calibrates `expected_min_score`; Phase 6 CLI-02 consumes it.

Output: Single YAML file with exactly 12 entries.
</objective>

<execution_context>
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/om.mengshetti/Desktop/tracer-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-research-design-artifacts/01-CONTEXT.md
@.planning/phases/01-research-design-artifacts/01-RESEARCH.md
@.planning/research/PITFALLS.md

<contract_note>
The 12 `doc_section` values authored here form the canonical taxonomy that Phase 3 CORP-01/02 (corpus ingester) MUST also use (Pitfall F in 01-RESEARCH.md). The exact 12 strings are: `auth`, `models`, `messages`, `tools`, `batches`, `files`, `citations`, `vision`, `errors-and-rate-limits`, `prompt-caching`, `agent-sdk-overview`, `agent-sdk-tools`. Do not change them in this phase — Phase 3 will use them as the chunker's `doc_section` taxonomy.
</contract_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/eval/coverage_set.yaml (12 hand-curated queries, DSGN-10)</name>
  <files>docs/eval/coverage_set.yaml</files>
  <read_first>
    - .planning/phases/01-research-design-artifacts/01-CONTEXT.md (D-31..D-35 — schema, 12-query set, hand-curated, no ground-truth answer)
    - .planning/phases/01-research-design-artifacts/01-RESEARCH.md §"Coverage Query Set Draft" (lines ~666-768; copy YAML verbatim)
    - .planning/research/PITFALLS.md §"Pitfall #10" — why proactive coverage is mandatory
  </read_first>
  <action>
Create the directory `docs/eval/` if it doesn't exist, then write `docs/eval/coverage_set.yaml`.

**Header comment block (must appear at top of file):**
```
# /docs/eval/coverage_set.yaml
# Proactive coverage regression query set for tracer-ai (DSGN-10)
# 12 queries — exceeds DSGN-10 floor of 10
#
# Authoring constraints (D-31 through D-35):
#   - Hand-curated against Claude API docs TOC (NOT LLM-generated)
#   - No ground-truth answer text — asserts retrieval coverage only
#   - expected_min_score: 0.6 (initial; calibrated in Phase 5 EVAL-06)
#
# Schema per entry (D-32):
#   id: COV-NN (zero-padded 2-digit, sequential)
#   query: str — the user-facing question text
#   doc_section: str — canonical taxonomy (must match Phase 3 chunker)
#   expected_chunk_keywords: list[str] — at least one MUST appear in retrieved chunks
#   expected_min_score: float — initial 0.6; tuned by EVAL-06
#   notes: str — provenance / source URL hint
#
# Phase 6 CLI-02 (eval/regression.py) loads this file via yaml.safe_load()
# and asserts retrieval coverage per query. Phase 5 may rewrite
# expected_min_score and expected_chunk_keywords during calibration.
```

**12 entries** — copy the YAML list from `01-RESEARCH.md §"Coverage Query Set Draft"` (lines ~679-761) verbatim. Each entry has fields `id`, `query`, `doc_section`, `expected_chunk_keywords` (YAML list), `expected_min_score: 0.6`, `notes`. The 12 IDs are COV-01..COV-12 mapped to the 12 doc sections in this exact order:

1. **COV-01** — auth (Anthropic Messages API authentication; keywords `x-api-key, authentication, API key, Authorization`)
2. **COV-02** — models (available Claude models + context window sizes; keywords `claude-sonnet-4-5, claude-haiku, context window, 200k`)
3. **COV-03** — messages (basic prompt structure; keywords `messages.create, role, content, user, assistant`)
4. **COV-04** — tools (tool use / function calling; keywords `tools, tool_use, input_schema, tool_result`)
5. **COV-05** — batches (Message Batches API; keywords `batches, Message Batches, 50% discount, 24 hours`)
6. **COV-06** — files (Files API; keywords `Files API, file_id, upload, type: file`)
7. **COV-07** — citations (cited_text + supported_text; keywords `citations, cited_text, document_index, supported_text`)
8. **COV-08** — vision (image input to Claude; keywords `vision, image, base64, media_type, image_url`)
9. **COV-09** — errors-and-rate-limits (HTTP error codes + 429; keywords `429, rate_limit_error, retry-after, tokens per minute, TPM`)
10. **COV-10** — prompt-caching (cache_control + savings; keywords `cache_control, ephemeral, cache hit, 90%, 5 minutes`)
11. **COV-11** — agent-sdk-overview (Claude Agent SDK overview; keywords `Agent SDK, claude-agent-sdk, subagents, harness, ClaudeAgentOptions`)
12. **COV-12** — agent-sdk-tools (custom MCP tools via SDK; keywords `MCP, Model Context Protocol, tool, in-process, create_sdk_mcp_server`)

Each entry uses YAML list format with `expected_chunk_keywords: ["...", "...", ...]` (inline list — readable in any YAML viewer). All `expected_min_score` values are `0.6`. The `notes` field references the source Claude docs URL section (e.g., `notes: "Covers /api/getting-started auth section."` for COV-01).

**Validation**: After writing, the file must parse as a YAML list of 12 mappings, each with the 6 required fields. The 12 `doc_section` values must be exactly the 12 strings listed above (no `vision-and-multimodal` typo, no underscore vs hyphen variation).
  </action>
  <verify>
    <automated>test -f docs/eval/coverage_set.yaml && python -c "import yaml,sys; data=yaml.safe_load(open('docs/eval/coverage_set.yaml')); assert isinstance(data, list), 'top-level must be a list'; assert len(data) == 12, f'expected 12 entries, got {len(data)}'; required={'id','query','doc_section','expected_chunk_keywords','expected_min_score','notes'}; sections=set(); ids=set();
import sys
for i, e in enumerate(data):
    missing = required - set(e.keys())
    assert not missing, f'entry {i}: missing fields {missing}'
    assert isinstance(e['expected_chunk_keywords'], list), f'entry {i}: expected_chunk_keywords must be a list'
    assert isinstance(e['expected_min_score'], (int, float)), f'entry {i}: expected_min_score must be a number'
    sections.add(e['doc_section'])
    ids.add(e['id'])
expected_sections={'auth','models','messages','tools','batches','files','citations','vision','errors-and-rate-limits','prompt-caching','agent-sdk-overview','agent-sdk-tools'}
assert sections == expected_sections, f'doc_section mismatch: missing {expected_sections - sections}, extra {sections - expected_sections}'
expected_ids={f'COV-{i:02d}' for i in range(1,13)}
assert ids == expected_ids, f'id mismatch: missing {expected_ids - ids}, extra {ids - expected_ids}'
print('YAML coverage set valid: 12 entries, all 12 doc_sections present, IDs COV-01..COV-12')
"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path `docs/eval/coverage_set.yaml`
    - File parses as a YAML list of exactly 12 mappings
    - Every entry has the 6 required fields: id, query, doc_section, expected_chunk_keywords (list), expected_min_score (number), notes
    - The set of `doc_section` values is exactly: {auth, models, messages, tools, batches, files, citations, vision, errors-and-rate-limits, prompt-caching, agent-sdk-overview, agent-sdk-tools}
    - The set of `id` values is exactly: {COV-01, COV-02, ..., COV-12}
    - Header comment block cites DSGN-10 and lists D-31..D-35 constraints
  </acceptance_criteria>
  <done>docs/eval/coverage_set.yaml committed with 12 entries spanning all 12 canonical doc_sections; file passes the Python yaml.safe_load + structural assertions in the verify step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author -> YAML file | Sole boundary |
| Phase 1 (this plan) -> Phase 3 corpus ingester | The doc_section taxonomy is the contract |
| Phase 1 (this plan) -> Phase 6 CLI-02 regression runner | The YAML schema is the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | doc_section taxonomy drift between Phase 1 and Phase 3 | mitigate | Pitfall F flagged in 01-RESEARCH.md; the 12 strings are pinned in this plan's action; Phase 3 plan must reference this file. |
| T-01-03-02 | Tampering | YAML syntax error breaks Phase 6 CLI-02 silently | mitigate | Verify step uses Python `yaml.safe_load` + structural assertions. |
| T-01-03-03 | Information Disclosure | Real API key embedded in `query` or `notes` | mitigate | All 12 queries are user-facing question text — no secrets. |
</threat_model>

<verification>
- File exists, parses as valid YAML
- 12 entries, all required fields, exact doc_section taxonomy, exact ID range
</verification>

<success_criteria>
- DSGN-10 satisfied (12 queries, exceeds 10-query floor)
- File ready for Phase 6 CLI-02 consumption
- Fresh-agent docs check Wave 4 doesn't directly query this file (it's not in the 5 questions), but the file's existence + parseability is a separate Phase 1 deliverable
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-design-artifacts/01-03-SUMMARY.md` with: file path, line count, the 12 doc_sections covered, and any phrasing refinements made vs the draft in 01-RESEARCH.md (none expected).
</output>
