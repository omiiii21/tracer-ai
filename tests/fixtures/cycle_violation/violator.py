"""Deliberate DAG violation fixture (per RESEARCH.md Validation Architecture).

This file is a placeholder; tests/test_import_cycle_guard.py builds a real
tracer_ai/-shaped tree under tmp_path and runs the guard against it. The
fixture's purpose is to anchor the directory in the test layout so the
package marker and structure are tracked under git.
"""
